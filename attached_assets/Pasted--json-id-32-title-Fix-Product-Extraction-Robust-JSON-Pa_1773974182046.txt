```json
{
  "id": 32,
  "title": "Fix Product Extraction: Robust JSON Parsing, Improved Prompt, and Error Recovery",
  "priority": "P1-High",
  "context": "The product extraction pipeline (`npm run extract-products`) is failing because DeepSeek returns malformed JSON responses – unterminated strings, missing quotes, etc. This causes the entire chunk to error out, resulting in zero products extracted. Logs show `AbortError` timeouts are reduced, but JSON parsing errors persist. We need a robust solution that can recover from malformed JSON, provide better logging, and instruct DeepSeek more clearly.",
  "task": "Implement a three-part fix: (1) a lenient JSON parser with fallback extraction, (2) an improved system prompt with strict JSON formatting rules, and (3) enhanced error logging to diagnose issues. The goal is to ensure extraction succeeds even with imperfect AI responses.",
  "detailed_instructions": [
    "## 1. Lenient JSON Parser with Fallback",
    "- In `server/lib/extract-products.ts`, locate the function `callDeepSeekWithRetry` (or similar) that calls the DeepSeek API and parses the response.",
    "- Replace the simple `JSON.parse()` with a robust `parseDeepSeekResponse(content: string): any[]` function that implements the following logic:",
    "  ```typescript",
    "  function parseDeepSeekResponse(content: string): any[] {",
    "    // Try strict parsing first",
    "    try {",
    "      const parsed = JSON.parse(content);",
    "      if (Array.isArray(parsed)) return parsed;",
    "      // If parsed is an object with a products field, extract it",
    "      if (parsed && Array.isArray(parsed.products)) return parsed.products;",
    "    } catch (e) {",
    "      // Log the failure for debugging",
    "      console.debug(`[DeepSeek] Strict JSON parse failed: ${e.message}`);",
    "    }",
    "",
    "    // Attempt to extract JSON array using regex",
    "    const arrayMatch = content.match(/\\[\\s*\\{.*?\\}\\s*\\]/s);",
    "    if (arrayMatch) {",
    "      try {",
    "        const extracted = JSON.parse(arrayMatch[0]);",
    "        if (Array.isArray(extracted)) return extracted;",
    "      } catch (e) {",
    "        console.debug(`[DeepSeek] Extracted array parse failed: ${e.message}`);",
    "      }",
    "    }",
    "",
    "    // Attempt to extract a JSON object that might contain products",
    "    const objectMatch = content.match(/\\{[\\s\\S]*?\\}/);",
    "    if (objectMatch) {",
    "      try {",
    "        const extracted = JSON.parse(objectMatch[0]);",
    "        if (extracted && Array.isArray(extracted.products)) return extracted.products;",
    "      } catch (e) {}",
    "    }",
    "",
    "    // If all else fails, log the problematic response snippet and return empty array",
    "    console.error(`[DeepSeek] Failed to parse response. Snippet: ${content.slice(0, 500)}...`);",
    "    return [];",
    "  }",
    "  ```",
    "- Use this function when processing each chunk's response. If it returns an empty array, log a warning but continue processing other chunks.",
    
    "## 2. Improved System Prompt with Strict JSON Formatting Rules",
    "- In the same file, find the system prompt sent to DeepSeek (likely a constant like `SYSTEM_PROMPT`).",
    "- Replace it with a more explicit version that emphasizes strict JSON output and provides a concrete example:",
    "  ```text",
    "  You are a helpful assistant that extracts menu items from restaurant menus. Your output must be ONLY a valid JSON array of objects, with no additional text, markdown, or commentary. Each object must follow this exact structure:",
    "  {",
    "    \"name\": \"string (required, max 100 chars)\",",
    "    \"description\": \"string or null (max 500 chars)\",",
    "    \"price\": number (positive, max 999.99, two decimals),",
    "    \"category\": \"one of: Pizza, Pasta, Ice Cream, Drinks, Appetizers, Desserts, Other\",",
    "    \"allergens\": array of strings (e.g., [\"Gluten\", \"Lactose\"]),",
    "    \"tags\": array of strings from: vegetarian, vegan, gluten-free, spicy, popular, seasonal",
    "    \"image_url\": \"string or null (must be a valid URL if provided)\"",
    "  }",
    "  Example:",
    "  [{\"name\":\"Margherita\",\"description\":\"Classic tomato and mozzarella\",\"price\":8.50,\"category\":\"Pizza\",\"allergens\":[\"Lactose\",\"Gluten\"],\"tags\":[\"vegetarian\"],\"image_url\":null}]",
    "  STRICT RULES:",
    "  - Escape all double quotes inside strings with backslash.",
    "  - Do not include trailing commas.",
    "  - Ensure all strings are properly terminated.",
    "  - If you are uncertain about a field, omit it rather than guessing.",
    "  - Return ONLY the JSON array, nothing else.",
    "  ```",
    "- Also include a user message that clearly states the task: `Extract products from this menu text:\n\n${textChunk}`.",
    
    "## 3. Enhanced Error Logging",
    "- Add detailed logging around the DeepSeek call and response:",
    "  ```typescript",
    "  console.log(`[DeepSeek] Sending chunk of length ${textChunk.length} chars`);",
    "  const start = Date.now();",
    "  const response = await callDeepSeekWithRetry(textChunk, signal);",
    "  const duration = Date.now() - start;",
    "  console.log(`[DeepSeek] Received response (${duration}ms), length: ${response.length} chars`);",
    "  ```",
    "- In the `parseDeepSeekResponse` function, log when the fallback mechanisms are used:",
    "  ```typescript",
    "  console.debug(`[DeepSeek] Strict parse failed, attempting extraction`);",
    "  ```",
    "- When returning an empty array, log a warning with a snippet of the response to aid debugging.",
    
    "## 4. Update the Extraction Script to Handle Empty Results Gracefully",
    "- In the main extraction loop, when a chunk returns an empty array, do not treat it as an error. Instead, log `[Extractor] No products found in chunk` and continue.",
    "- Ensure the final summary counts only successfully parsed products, and any chunks that failed completely are counted in an `errors` array.",
    
    "## 5. Testing and Verification",
    "- Run `npm run extract-products:dry` to test the new parsing logic without writing to the database. Check the console logs for any remaining JSON errors.",
    "- Run the full extraction and verify that products are inserted/updated correctly.",
    "- Test with a known problematic PDF (if available) to ensure the fallback parser recovers.",
    "- Verify that the admin 'Run Extraction' button now shows a success toast with the correct product counts."
  ],
  "replit_note": "The changes are isolated to `server/lib/extract-products.ts` and possibly `scripts/extract-products.ts`. No database schema changes are required. After implementing, run the extraction and monitor the logs for any remaining issues. The improved logging will help identify any further problems.",
  "testing_checkpoint": [
    "1. Run `npm run extract-products:dry` – observe logs; no JSON parse errors should appear.",
    "2. Run `npm run extract-products` – verify that products are inserted/updated.",
    "3. Check the database: `SELECT COUNT(*) FROM products;` should match expected number.",
    "4. In admin, click 'Run Extraction' – success toast appears with correct counts."
  ]
}
```