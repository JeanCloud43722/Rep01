{
  "meta": {
    "prompt_id": "29-v2.0-Final",
    "role": "Senior Backend & AI Engineer",
    "purpose": "Implement secure, variant-aware DeepSeek chat ordering fully synced with Prompt 27's dynamic backend and Prompt 28's cart-to-chat injection",
    "prerequisites": [
      "Prompt 27: PDF extraction with dynamic categories, variants JSONB, WebSocket MENU_UPDATED broadcasts",
      "Prompt 28: ProductCatalog with variant-aware modals, cart sidebar, chat injection via onSendToChat",
      "Existing: WebSocket pub/sub, rate limiting middleware (Prompt 5), session auth, sanitizeMessage utility"
    ],
    "scope": "STRICTLY: Chat endpoint, DeepSeek tool calling, order confirmation flow, idempotency, admin integration. NO UI components, NO audio/push modifications."
  },
  
  "critical_directive": "⚠️ SENIOR ENGINEER MODE: This is the ORDERING BRAIN. It must: (1) understand natural language OR pre-compiled cart injections from Prompt 28, (2) resolve product variants from Prompt 27's JSONB, (3) NEVER trust client prices, (4) enforce idempotency via UUIDs, (5) upsell drinks when only food is ordered. If you start modifying UI components or audio/push code, STOP immediately – those belong to other prompts.",
  
  "project_context": {
    "name": "Bistro Buzzer",
    "platform": "Replit (Node.js/Express, PostgreSQL/Neon, Drizzle ORM, TypeScript, DeepSeek API)",
    "data_contract_from_prompt_27": {
      "product": {
        "id": "number",
        "name": "string",
        "category": "string (dynamic)",
        "variants": "Array<{ name: string, price: number, description?: string }> | null",
        "price": "number | null (legacy fallback)",
        "allergens": "string[]",
        "tags": "string[]"
      }
    },
    "input_contract_from_prompt_28": {
      "cart_injection_format": "Natural language string like: \"I'd like to order: 1x Pizza Margherita (Large, no onions), 2x Cola. Total: €24.50\"",
      "or_structured_json": "{\"intent\":\"order\",\"items\":[{\"product_id\":42,\"variant_name\":\"Large\",\"quantity\":1,\"modifications\":\"no onions\"}]}"
    }
  },
  
  "objective": "Implement a production-ready, secure AI chat ordering system that: (1) handles both natural language and cart-injected orders, (2) uses DeepSeek function calling with variant-aware product search, (3) enforces server-side price validation and idempotency, (4) implements intelligent upselling, and (5) broadcasts confirmed orders to admin dashboard via WebSocket.",
  
  "deliverables": [
    {
      "file": "shared/db-schema.ts",
      "task": "Add order_items and idempotency_keys tables with proper indexes",
      "code_requirements": [
        "export const orderItems = pgTable('order_items', {",
        "  id: serial('id').primaryKey(),",
        "  orderId: text('order_id').notNull().references(() => orders.id, { onDelete: 'cascade' }),",
        "  productId: integer('product_id').notNull().references(() => products.id, { onDelete: 'restrict' }),",
        "  variantName: text('variant_name'), // CRITICAL: stores size like 'Small', 'Large' from Prompt 27 variants",
        "  quantity: integer('quantity').notNull().default(1).check(sql`quantity > 0`),",
        "  modifications: text('modifications'), // sanitized user input",
        "  priceAtTime: numeric('price_at_time', { precision: 10, scale: 2 }).notNull(), // SNAPSHOT of DB price at order time",
        "  createdAt: timestamp('created_at').defaultNow()",
        "});",
        "",
        "// Composite index for fast order lookups",
        "export const orderItemsOrderIdx = index('order_items_order_idx').on(orderItems.orderId, orderItems.productId);",
        "",
        "// Idempotency keys table (prevents duplicate confirmations)",
        "export const idempotencyKeys = pgTable('idempotency_keys', {",
        "  key: text('key').primaryKey(), // client-generated UUID",
        "  orderId: text('order_id').notNull(),",
        "  createdAt: timestamp('created_at').defaultNow()",
        "});",
        "export const idempotencyKeysOrderIdx = index('idempotency_order_idx').on(idempotencyKeys.orderId);"
      ]
    },
    {
      "file": "server/lib/chat-tools.ts",
      "task": "Define and implement variant-aware search_products tool for DeepSeek",
      "code_requirements": [
        "export const searchProductsTool = {",
        "  type: 'function' as const,",
        "  function: {",
        "    name: 'search_products',",
        "    description: 'Search restaurant products by name, category, or tag. Returns full product data including variants for order construction.',",
        "    parameters: {",
        "      type: 'object',",
        "      properties: {",
        "        query: { type: 'string', description: 'Search query, e.g., \"vegetarian pizza\" or \"large cola\"' },",
        "        limit: { type: 'integer', description: 'Max results (default: 10)', default: 10 }",
        "      },",
        "      required: ['query'],",
        "      additionalProperties: false",
        "    }",
        "  }",
        "};",
        "",
        "export async function executeSearchProducts(query: string, limit: number, db: NeonQueryFn) {",
        "  // Parameterized query to prevent SQL injection",
        "  const results = await db.select()",
        "    .from(products)",
        "    .where(",
        "      or(",
        "        ilike(products.name, `%${query}%`),",
        "        ilike(products.description, `%${query}%`),",
        "        eq(products.category, query),",
        "        sql`${query} = ANY(${products.tags})`",
        "      )",
        "    )",
        "    .limit(limit);",
        "  ",
        "  // CRITICAL: Return variants JSONB array so AI can resolve size queries",
        "  return results.map(p => ({",
        "    id: p.id,",
        "    name: p.name,",
        "    category: p.category,",
        "    price: p.price ? Number(p.price) : null,",
        "    variants: p.variants, // JSONB array from Prompt 27 – essential for size resolution",
        "    allergens: p.allergens,",
        "    tags: p.tags",
        "  }));",
        "}"
      ]
    },
    {
      "file": "server/routes/chat.ts",
      "task": "Implement POST /api/orders/:orderId/chat with loop protection, timeout, and upselling logic",
      "code_requirements": [
        "Constants:",
        "const MAX_TOOL_ITERATIONS = 3; // Prevent infinite AI loops",
        "const DEEPSEEK_TIMEOUT_MS = 25000; // Safety margin for Replit free tier",
        "const MAX_MESSAGE_LENGTH = 2000;",
        "",
        "export async function handleChat(req: Request, res: Response) {",
        "  const { orderId } = req.params;",
        "  const { message } = req.body;",
        "  ",
        "  // 1. Input validation & order state check",
        "  if (!message || typeof message !== 'string' || message.length > MAX_MESSAGE_LENGTH) {",
        "    return res.status(400).json({ error: { code: 'INVALID_INPUT', message: 'Message required, max 2000 chars' } });",
        "  }",
        "  const order = await storage.getOrder(orderId);",
        "  if (!order) return res.status(404).json({ error: 'Order not found' });",
        "  if (order.status === 'notified' || order.status === 'completed') {",
        "    return res.status(400).json({ error: { code: 'ORDER_COMPLETED', message: 'Order already completed; use chat to modify before confirmation' } });",
        "  }",
        "  ",
        "  // 2. Fetch recent conversation history (last 10 messages to control token usage)",
        "  const history = await getRecentMessages(orderId, 10);",
        "  ",
        "  // 3. DeepSeek call loop with timeout and iteration guard",
        "  const controller = new AbortController();",
        "  const timeout = setTimeout(() => controller.abort(), DEEPSEEK_TIMEOUT_MS);",
        "  ",
        "  let iteration = 0;",
        "  let currentMessage = message;",
        "  let toolResults: any[] = [];",
        "  let finalReply = '';",
        "  let orderPreview: OrderPreview | null = null;",
        "  ",
        "  try {",
        "    while (iteration < MAX_TOOL_ITERATIONS) {",
        "      const response = await callDeepSeek({",
        "        model: 'deepseek-chat',",
        "        messages: [",
        "          { role: 'system', content: SYSTEM_PROMPT },",
        "          ...history,",
        "          { role: 'user', content: currentMessage }",
        "        ],",
        "        tools: [searchProductsTool],",
        "        tool_choice: 'auto',",
        "        temperature: 0.1,",
        "        response_format: { type: 'text' } // We parse JSON manually for flexibility",
        "      }, controller.signal);",
        "      ",
        "      // Check if AI wants to call a tool",
        "      if (response.tool_calls?.length) {",
        "        for (const toolCall of response.tool_calls) {",
        "          if (toolCall.function.name === 'search_products') {",
        "            const args = JSON.parse(toolCall.function.arguments);",
        "            const results = await executeSearchProducts(args.query, args.limit || 10, db);",
        "            toolResults.push({ tool_call_id: toolCall.id, results });",
        "          }",
        "        }",
        "        // Continue loop with tool results injected as assistant message",
        "        currentMessage = JSON.stringify({ tool_results: toolResults });",
        "        iteration++;",
        "        continue;",
        "      }",
        "      ",
        "      // No more tool calls – parse final reply for order JSON",
        "      finalReply = response.content || '';",
        "      orderPreview = extractOrderJson(finalReply);",
        "      break;",
        "    }",
        "  } catch (err: any) {",
        "    if (err.name === 'AbortError') {",
        "      console.warn(`[Chat] DeepSeek timeout for order ${orderId}`);",
        "      return res.json({",
        "        reply: \"I'm taking a bit long to process – could you rephrase or try again?\",\n        requires_clarification: true,",
        "        meta: { error: 'timeout' }",
        "      });",
        "    }",
        "    throw err;",
        "  } finally {",
        "    clearTimeout(timeout);",
        "  }",
        "  ",
        "  // 4. Handle iteration limit exceeded",
        "  if (iteration >= MAX_TOOL_ITERATIONS) {",
        "    return res.json({",
        "      reply: \"I want to make sure I get your order right. Could you clarify the items or sizes?\",\n      requires_clarification: true,",
        "      meta: { iterations_used: MAX_TOOL_ITERATIONS }",
        "    });",
        "  }",
        "  ",
        "  // 5. Intelligent upselling: if order contains only food, suggest a drink",
        "  if (orderPreview?.items?.length && !orderPreview.requires_clarification) {",
        "    const hasDrink = orderPreview.items.some(item => {",
        "      const product = products.find(p => p.id === item.product_id);",
        "      return product?.category?.toLowerCase().includes('drink') || product?.tags?.includes('beverage');",
        "    });",
        "    if (!hasDrink) {",
        "      // Append gentle upsell suggestion to reply",
        "      finalReply += \"\\n\\n💡 Would you like to add a drink to your order? We have Cola, Water, and fresh Lemonade.\";",
        "    }",
        "  }",
        "  ",
        "  // 6. Return response with optional order preview",
        "  return res.json({",
        "    reply: finalReply,",
        "    order_preview: orderPreview, // null if no valid JSON found",
        "    meta: { iterations_used: iteration, has_order_json: !!orderPreview }",
        "  });",
        "}",
        "",
        "// Helper: robust JSON extraction with 3 fallback strategies",
        "function extractOrderJson(text: string): OrderPreview | null {",
        "  try {",
        "    // Strategy 1: Look for ```json code block",
        "    const codeBlock = text.match(/```json\\n([\\s\\S]*?)\\n```/);",
        "    if (codeBlock) {",
        "      const parsed = JSON.parse(codeBlock[1]);",
        "      return validateOrderPreview(parsed) ? parsed : null;",
        "    }",
        "    // Strategy 2: Look for loose JSON with required fields",
        "    const looseJson = text.match(/\\{[\\s\\S]*\"intent\"[\\s\\S]*\"order\"[\\s\\S]*\\}/);",
        "    if (looseJson) {",
        "      const parsed = JSON.parse(looseJson[0]);",
        "      return validateOrderPreview(parsed) ? parsed : null;",
        "    }",
        "    // Strategy 3: Try parsing entire message",
        "    const parsed = JSON.parse(text);",
        "    return validateOrderPreview(parsed) ? parsed : null;",
        "  } catch {",
        "    return null;",
        "  }",
        "}",
        "",
        "function validateOrderPreview(obj: any): obj is OrderPreview {",
        "  return (",
        "    obj &&",
        "    obj.intent === 'order' &&",
        "    Array.isArray(obj.items) &&",
        "    obj.items.every((item: any) =>",
        "      item.product_id && typeof item.quantity === 'number' && item.quantity > 0 && item.quantity <= 10",
        "    )",
        "  );",
        "}"
      ]
    },
    {
      "file": "server/lib/prompts.ts",
      "task": "Craft precise system prompt for DeepSeek with variant handling and upselling instructions",
      "content": [
        "export const SYSTEM_PROMPT = `",
        "You are BistroBot, a friendly, efficient ordering assistant for a restaurant. Your goals:",
        "",
        "1. UNDERSTAND INPUT: The user may type naturally OR send a pre-compiled cart list from the visual menu. Handle both gracefully.",
        "",
        "2. SEARCH PRODUCTS: Use the search_products tool when the user mentions food/drink names. The tool returns products with a 'variants' array (e.g., [{name:'Small',price:8.00},{name:'Large',price:12.00}]).",
        "",
        "3. RESOLVE VARIANTS: If a product has multiple variants (sizes) and the user didn't specify which one, ASK for clarification BEFORE confirming. Example: 'Would you like Small (€8) or Large (€12)?'",
        "",
        "4. CLARIFY AMBIGUITIES: If uncertain about quantity, modifications, or item identity, ask concise follow-up questions. Never guess.",
        "",
        "5. GENERATE ORDER JSON: When the order is clear and the user indicates confirmation, output a structured JSON block enclosed in ```json ... ``` with this exact format:",
        "{",
        "  \"intent\": \"order\",",
        "  \"items\": [",
        "    {",
        "      \"product_id\": 42, // USE THE EXACT ID FROM search_products RESULTS – NEVER HALLUCINATE",
        "      \"name\": \"Pizza Margherita\",",
        "      \"variant_name\": \"Large\", // OPTIONAL: include if variant was selected",
        "      \"quantity\": 2,",
        "      \"modifications\": \"no onions\" // OPTIONAL: user requests",
        "    }",
        "  ]",
        "}",
        "Rules for JSON:",
        "- product_id MUST come from search_products results",
        "- variant_name MUST match exactly one of the variant.name values from the product's variants array",
        "- quantity must be 1-10",
        "- Do NOT include price in the JSON – the server validates prices",
        "",
        "6. INTELLIGENT UPSELLING: If the order contains only food items (no drinks), gently suggest adding a beverage before finalizing: '💡 Would you like to add a drink? We have Cola, Water, and fresh Lemonade.'",
        "",
        "7. KEEP IT CONCISE: Be friendly but efficient. Use the customer's language if detectable.",
        "",
        "Example successful flow:",
        "User: \"I want two margarita pizzas\"",
        "Assistant: [calls search_products with query=\"margarita pizza\"]",
        "Assistant: \"I found Pizza Margherita. It comes in Small (€8.50) or Large (€12.50). Which size would you like, and how many?\"",
        "User: \"2 Large please, no olives\"",
        "Assistant: \"Perfect! Here's your order summary:",
        "```json",
        "{\"intent\":\"order\",\"items\":[{\"product_id\":42,\"name\":\"Pizza Margherita\",\"variant_name\":\"Large\",\"quantity\":2,\"modifications\":\"no olives\"}]}",
        "```",
        "Please confirm to place this order. 💡 Would you like to add a drink? We have Cola, Water, and fresh Lemonade.\"",
        "`;"
      ]
    },
    {
      "file": "client/src/components/OrderConfirmation.tsx",
      "task": "Frontend component that renders when chat detects JSON block, with idempotency key generation",
      "code_requirements": [
        "Props: {",
        "  orderId: string;",
        "  preview: OrderPreview;",
        "  onConfirm: (payload: ConfirmPayload) => Promise<void>;",
        "  onCancel: () => void;",
        "}",
        "",
        "interface ConfirmPayload {",
        "  items: Array<{ product_id: number; variant_name?: string; quantity: number; modifications?: string }>;",
        "  idempotencyKey: string; // client-generated UUID",
        "  clientTimestamp: number;",
        "}",
        "",
        "Component logic:",
        "```typescript",
        "export function OrderConfirmation({ orderId, preview, onConfirm, onCancel }: OrderConfirmationProps) {",
        "  const [isConfirming, setIsConfirming] = useState(false);",
        "  const [error, setError] = useState<string | null>(null);",
        "  ",
        "  // Generate UUID for idempotency (use crypto.randomUUID() or uuid library)",
        "  const idempotencyKey = useMemo(() => crypto.randomUUID(), []);",
        "  ",
        "  const handleConfirm = async () => {",
        "    setIsConfirming(true);",
        "    setError(null);",
        "    ",
        "    try {",
        "      await onConfirm({",
        "        items: preview.items,",
        "        idempotencyKey,",
        "        clientTimestamp: Date.now()",
        "      });",
        "      // Success: show confirmation message, optionally auto-switch to status tab",
        "    } catch (err: any) {",
        "      // Handle duplicate confirmation gracefully",
        "      if (err?.duplicate) {",
        "        setError('Order already confirmed. Refreshing...');",
        "        // Optionally trigger cache invalidation",
        "      } else {",
        "        setError(err.message || 'Could not confirm order. Tap to retry.');",
        "      }",
        "    } finally {",
        "      setIsConfirming(false);",
        "    }",
        "  };",
        "  ",
        "  // Calculate client-side estimate for display (NOT sent to server)",
        "  const estimatedTotal = preview.items.reduce((sum, item) => {",
        "    // Note: actual price is validated server-side",
        "    return sum + (item.estimated_price || 0) * item.quantity;",
        "  }, 0);",
        "  ",
        "  return (",
        "    <div className=\"glass rounded-xl p-4 my-3\" role=\"region\" aria-label=\"Order confirmation\">",
        "      <h4 className=\"font-semibold text-lg mb-2\" id=\"confirmation-title\">Confirm your order</h4>",
        "      <ul className=\"space-y-2 mb-4\">",
        "        {preview.items.map((item, idx) => (",
        "          <li key={idx} className=\"flex justify-between text-sm\">",
        "            <span>",
        "              {item.quantity}x {item.name}",
        "              {item.variant_name && <span className=\"text-gray-500\"> ({item.variant_name})</span>}",
        "              {item.modifications && <span className=\"text-gray-400 italic\"> – {item.modifications}</span>}",
        "            </span>",
        "            <span className=\"font-medium\">€{(item.estimated_price || 0).toFixed(2)}</span>",
        "          </li>",
        "        ))}",
        "      </ul>",
        "      <div className=\"flex justify-between font-semibold mb-4\">",
        "        <span>Estimated total</span>",
        "        <span>€{estimatedTotal.toFixed(2)}</span>",
        "      </div>",
        "      {error && <p className=\"text-red-600 text-sm mb-3\" role=\"alert\">{error}</p>}",
        "      <div className=\"flex gap-2\">",
        "        <button",
        "          onClick={onCancel}",
        "          disabled={isConfirming}",
        "          className=\"flex-1 px-4 py-2 glass-hover rounded-lg\"",
        "        >",
        "          Change",
        "        </button>",
        "        <button",
        "          onClick={handleConfirm}",
        "          disabled={isConfirming}",
        "          className=\"flex-1 bg-green-600 hover:bg-green-700 disabled:bg-gray-400 text-white font-semibold px-4 py-2 rounded-lg transition-colors\"",
        "        >",
        "          {isConfirming ? 'Confirming...' : '✓ Confirm Order'}",
        "        </button>",
        "      </div>",
        "      <p className=\"text-xs text-gray-400 mt-3\">",
        "        Prices are validated server-side. Final total may differ slightly.",
        "      </p>",
        "    </div>",
        "  );",
        "}",
        "```",
        "",
        "Accessibility:",
        "- `role=\"region\"` with `aria-label` for screen readers",
        "- Error messages use `role=\"alert\"` for immediate announcement",
        "- Buttons have clear labels and disabled states"
      ]
    },
    {
      "file": "server/routes/orders.ts",
      "task": "Implement POST /api/orders/:orderId/confirm-order with idempotency, price validation, and transaction safety",
      "code_requirements": [
        "export async function confirmOrder(req: Request, res: Response) {",
        "  const { orderId } = req.params;",
        "  const { items, idempotencyKey, clientTimestamp } = req.body;",
        "  ",
        "  // 1. Input validation",
        "  if (!Array.isArray(items) || items.length === 0) {",
        "    return res.status(400).json({ error: { code: 'INVALID_ITEMS', message: 'At least one item required' } });",
        "  }",
        "  if (!idempotencyKey || typeof idempotencyKey !== 'string') {",
        "    return res.status(400).json({ error: { code: 'MISSING_IDEMPOTENCY', message: 'Idempotency key required' } });",
        "  }",
        "  ",
        "  // 2. Idempotency check: if key exists within last 10 minutes, return success without processing",
        "  const existingKey = await db.query.idempotencyKeys.findFirst({",
        "    where: and(",
        "      eq(idempotencyKeys.key, idempotencyKey),",
        "      sql`${idempotencyKeys.createdAt} > NOW() - INTERVAL '10 minutes'`",
        "    )",
        "  });",
        "  if (existingKey) {",
        "    console.log(`[Idempotency] Duplicate confirmation for order ${orderId}, key ${idempotencyKey}`);",
        "    return res.json({ success: true, duplicate: true, message: 'Order already confirmed' });",
        "  }",
        "  ",
        "  // 3. CRITICAL SECURITY: Validate each item against DB, enforce server-side pricing",
        "  const validatedItems = await Promise.all(items.map(async (item: any, idx: number) => {",
        "    // Fetch product with variants from DB",
        "    const product = await db.query.products.findFirst({",
        "      where: eq(products.id, item.product_id)",
        "    });",
        "    if (!product) {",
        "      throw new Error(`Product ${item.product_id} not found`);",
        "    }",
        "    ",
        "    // Resolve price: if variant specified, find it in JSONB array; else use base price",
        "    let priceAtTime: number;",
        "    let resolvedVariantName: string | null = null;",
        "    ",
        "    if (item.variant_name && product.variants && Array.isArray(product.variants)) {",
        "      const variant = product.variants.find((v: any) => v.name === item.variant_name);",
        "      if (!variant) {",
        "        throw new Error(`Variant '${item.variant_name}' not found for product ${product.name}`);",
        "      }",
        "      priceAtTime = Number(variant.price);",
        "      resolvedVariantName = variant.name;",
        "    } else if (product.price) {",
        "      priceAtTime = Number(product.price);",
        "    } else {",
        "      throw new Error(`No price available for product ${product.name}`);",
        "    }",
        "    ",
        "    // Enforce quantity limits (prevent abuse)",
        "    const MAX_QTY = 10;",
        "    if (!Number.isInteger(item.quantity) || item.quantity < 1 || item.quantity > MAX_QTY) {",
        "      throw new Error(`Quantity must be 1-${MAX_QTY}`);",
        "    }",
        "    ",
        "    // Sanitize modifications using existing utility",
        "    const modifications = item.modifications ? sanitizeMessage(String(item.modifications)).slice(0, 200) : null;",
        "    ",
        "    return {",
        "      orderId,",
        "      productId: item.product_id,",
        "      variantName: resolvedVariantName,",
        "      quantity: item.quantity,",
        "      modifications,",
        "      priceAtTime",
        "    };",
        "  }));",
        "  ",
        "  // 4. Transaction: insert all items atomically",
        "  try {",
        "    await db.transaction(async (tx) => {",
        "      // Insert order_items",
        "      for (const item of validatedItems) {",
        "        await tx.insert(orderItems).values(item);",
        "      }",
        "      ",
        "      // Store idempotency key (with implicit 10-min TTL via query filter)",
        "      await tx.insert(idempotencyKeys).values({ key: idempotencyKey, orderId });",
        "      ",
        "      // Optional: update order status if transitioning from 'waiting' to 'subscribed'",
        "      const order = await tx.query.orders.findFirst({ where: eq(orders.id, orderId) });",
        "      if (order?.status === 'waiting') {",
        "        await tx.update(orders).set({ status: 'subscribed', updatedAt: new Date() }).where(eq(orders.id, orderId));",
        "      }",
        "    });",
        "  } catch (err: any) {",
        "    console.error(`[ConfirmOrder] Transaction failed for order ${orderId}:`, err);",
        "    return res.status(500).json({ error: { code: 'DB_ERROR', message: 'Could not save order' } });",
        "  }",
        "  ",
        "  // 5. Broadcast to admin dashboard via WebSocket",
        "  await publishToChannel(`admin:orders:${orderId}`, {",
        "    type: 'ORDER_ITEMS_ADDED',",
        "    items: validatedItems.map(i => ({",
        "      productName: items.find(x => x.product_id === i.productId)?.name,",
        "      variantName: i.variantName,",
        "      quantity: i.quantity,",
        "      price: i.priceAtTime,",
        "      modifications: i.modifications",
        "    })),",
        "    timestamp: new Date().toISOString()",
        "  });",
        "  console.log(`[WebSocket] Broadcast ORDER_ITEMS_ADDED for order ${orderId}`);",
        "  ",
        "  // 6. Return sanitized confirmation",
        "  const total = validatedItems.reduce((sum, i) => sum + i.priceAtTime * i.quantity, 0);",
        "  return res.json({",
        "    success: true,",
        "    orderId,",
        "    total: Number(total.toFixed(2)),",
        "    items: validatedItems.map(i => ({",
        "      name: items.find(x => x.product_id === i.productId)?.name,",
        "      variantName: i.variantName,",
        "      quantity: i.quantity,",
        "      price: i.priceAtTime",
        "    }))",
        "  });",
        "}"
      ]
    },
    {
      "file": "client/src/pages/CustomerPage.tsx (integration snippet)",
      "task": "Wire chat response to OrderConfirmation component and confirmation endpoint",
      "code_requirements": [
        "// Inside chat message rendering logic:",
        "```typescript",
        "// When a new message arrives from AI",
        "if (message.sender === 'ai' && message.order_preview) {",
        "  // Render OrderConfirmation component below the chat bubble",
        "  return (",
        "    <OrderConfirmation",
        "      orderId={orderId}",
        "      preview={message.order_preview}",
        "      onConfirm={async (payload) => {",
        "        const response = await fetch(`/api/orders/${orderId}/confirm-order`, {",
        "          method: 'POST',",
        "          headers: { 'Content-Type': 'application/json' },",
        "          body: JSON.stringify(payload)",
        "        });",
        "        const result = await response.json();",
        "        if (!response.ok) {",
        "          throw result.error || new Error('Confirmation failed');",
        "        }",
        "        // If duplicate, still treat as success",
        "        if (result.duplicate) {",
        "          throw { duplicate: true };",
        "        }",
        "        // Success: add system message, optionally invalidate order query",
        "        addSystemMessage(`✅ Order confirmed! Total: €${result.total}`);",
        "        queryClient.invalidateQueries({ queryKey: ['order', orderId] });",
        "      }}",
        "      onCancel={() => {",
        "        // Send a follow-up message to AI indicating user wants to change",
        "        sendChatMessage(\"Actually, let me change my order.\");",
        "      }}",
        "    />",
        "  );",
        "}",
        "```",
        "",
        "// For cart injection from Prompt 28:",
        "// When CartSidebar calls onSendToChat(message), it triggers the same chat flow",
        "// The AI will parse the natural language/cart JSON and respond with confirmation JSON"
      ]
    },
    {
      "file": "client/src/components/admin/AdminOrderModal.tsx (integration snippet)",
      "task": "Display order_items with variant names and prices in admin dashboard",
      "code_requirements": [
        "// Fetch order items when modal opens",
        "```typescript",
        "const {  orderItems = [] } = useQuery({",
        "  queryKey: ['order-items', orderId],",
        "  queryFn: () => fetch(`/api/admin/orders/${orderId}/items`).then(r => r.json())",
        "});",
        "```",
        "",
        "// Render items table",
        "```tsx",
        "{orderItems.length > 0 && (",
        "  <div className=\"mt-4\">",
        "    <h4 className=\"font-semibold mb-2\">Order Items</h4>",
        "    <table className=\"w-full text-sm\">",
        "      <thead>",
        "        <tr className=\"text-left text-gray-500\">",
        "          <th className=\"py-2\">Item</th>",
        "          <th className=\"py-2\">Variant</th>",
        "          <th className=\"py-2\">Qty</th>",
        "          <th className=\"py-2\">Price</th>",
        "          <th className=\"py-2\">Modifications</th>",
        "        </tr>",
        "      </thead>",
        "      <tbody>",
        "        {orderItems.map((item: any) => (",
        "          <tr key={item.id} className=\"border-t\">",
        "            <td className=\"py-2\">{item.product_name}</td>",
        "            <td className=\"py-2 text-gray-600\">{item.variant_name || '–'}</td>",
        "            <td className=\"py-2\">{item.quantity}</td>",
        "            <td className=\"py-2 font-medium\">€{Number(item.price_at_time).toFixed(2)}</td>",
        "            <td className=\"py-2 text-gray-500 italic\">{item.modifications || '–'}</td>",
        "          </tr>",
        "        ))}",
        "      </tbody>",
        "    </table>",
        "    <div className=\"mt-2 font-semibold\">",
        "      Total: €{orderItems.reduce((sum: number, i: any) => sum + Number(i.price_at_time) * i.quantity, 0).toFixed(2)}",
        "    </div>",
        "  </div>",
        ")}",
        "```",
        "",
        "// Optional: visual indicator for AI-generated orders",
        "{orderItems.some((i: any) => i.source === 'ai_chat') && (",
        "  <span className=\"inline-flex items-center gap-1 text-xs text-blue-600 bg-blue-50 px-2 py-1 rounded-full\">",
        "    <Bot className=\"w-3 h-3\" /> AI-ordered",
        "  </span>",
        ")}"
      ]
    },
    {
      "file": "README.md",
      "task": "Add documentation section for AI chat ordering",
      "content": [
        "## AI Chat Ordering (Prompt 29)",
        "",
        "Customers can order via natural language chat or by injecting their cart from the visual menu (Prompt 28).",
        "",
        "### How It Works",
        "1. User types or sends cart: \"I want 2 Large Pizza Margherita, no onions\"",
        "2. AI uses `search_products` tool to find items, resolves variants (sizes)",
        "3. If order is clear, AI outputs structured JSON block for confirmation",
        "4. User clicks 'Confirm' – frontend sends payload with idempotency UUID",
        "5. Server validates prices against DB, inserts order_items in transaction",
        "6. Admin dashboard receives WebSocket update with item details",
        "",
        "### Security Measures",
        "- ✅ Prices are ALWAYS validated server-side – client cannot manipulate totals",
        "- ✅ Idempotency keys prevent duplicate confirmations (10-minute window)",
        "- ✅ Quantity limits (1-10) prevent abuse",
        "- ✅ Modifications input is sanitized via `sanitizeMessage` utility",
        "- ✅ DeepSeek calls have 25s timeout + max 3 tool-call iterations",
        "",
        "### Intelligent Upselling",
        "If an order contains only food items, the AI gently suggests adding a drink before finalizing.",
        "",
        "### Testing",
        "```bash",
        "# Test variant resolution",
        "curl -X POST https://your-repl.replit.app/api/orders/test-123/chat \\",
        "  -H 'Content-Type: application/json' \\",
        "  -d '{\"message\": \"I want a large cola\"}'",
        "",
        "# Test idempotency (send same confirmation twice with same UUID)",
        "# First call: inserts items",
        "# Second call: returns {\"success\":true,\"duplicate\":true}",
        "```",
        "",
        "### Admin Dashboard",
        "Order items appear in the admin modal with variant names, quantities, and server-validated prices. Export to CSV for kitchen tickets."
      ]
    }
  ],
  
  "integration_points": {
    "with_prompt_27_backend": [
      "Product search returns variants JSONB array – essential for AI to resolve size queries",
      "Dynamic categories: AI learns categories from search results, no hardcoded assumptions",
      "WebSocket broadcast: after successful confirmation, publish to admin channel for real-time dashboard update"
    ],
    "with_prompt_28_frontend": [
      "Cart injection: when Prompt 28's CartSidebar calls onSendToChat(message), this endpoint parses the natural language or structured JSON",
      "OrderConfirmation component: renders when AI response contains valid JSON block",
      "Idempotency: frontend generates UUID via crypto.randomUUID() and sends with confirmation payload"
    ],
    "with_existing_infrastructure": [
      "Rate limiting: reuse middleware from Prompt 5 on /chat and /confirm-order endpoints",
      "sanitizeMessage: use existing utility to clean modifications input server-side",
      "WebSocket pub/sub: reuse wsManager.publishToChannel() for admin notifications",
      "storage.getOrder: use existing order retrieval logic with status checks"
    ]
  },
  
  "implementation_constraints": [
    "✅ Variant resolution: if product.variants exists, AI MUST ask for size unless user specified",
    "✅ Server-side pricing: NEVER use client-provided price – always query DB and use product.price or variant.price",
    "✅ Idempotency: store client-generated UUID, return {duplicate:true} if seen within 10 minutes",
    "✅ Transaction safety: insert all order_items atomically with Drizzle transaction",
    "✅ Upselling logic: if order has only food (no drinks), append gentle drink suggestion to AI reply",
    "✅ Loop protection: max 3 tool-call iterations per request to prevent runaway API costs",
    "✅ Timeout safety: AbortController with 25s timeout for all DeepSeek calls",
    "✅ XSS protection: sanitize modifications input via existing sanitizeMessage utility",
    "✅ Rate limiting: reuse existing middleware – do not implement new limiter",
    "✅ TypeScript: strict types, no any, proper interfaces for OrderPreview, ConfirmPayload, etc.",
    "❌ NO UI component modifications beyond OrderConfirmation integration",
    "❌ NO audio/push/notification code changes (handled in other prompts)",
    "❌ NO hardcoded categories or product lists in system prompt – keep generic for SaaS"
  ],
  
  "testing_checkpoints": [
    {
      "step": 1,
      "command": "Chat: \"I want a Margherita pizza\"",
      "expected": "AI calls search_products, sees variants array, replies asking 'Small (€8) or Large (€12)?' – does NOT output JSON yet"
    },
    {
      "step": 2,
      "command": "Chat: \"1x Large Margherita, no onions\"",
      "expected": "AI outputs JSON block with product_id, variant_name:'Large', quantity:1, modifications:'no onions'"
    },
    {
      "step": 3,
      "command": "Click 'Confirm Order' in frontend",
      "expected": "POST /confirm-order receives payload with idempotencyKey; server validates price from DB variants array; order_items inserted with correct priceAtTime"
    },
    {
      "step": 4,
      "command": "Send same confirmation payload twice with same idempotencyKey",
      "expected": "First call: inserts items, returns success. Second call: returns {success:true,duplicate:true} without inserting duplicates"
    },
    {
      "step": 5,
      "command": "Admin dashboard: open confirmed order modal",
      "expected": "Order items table shows 'Pizza Margherita', variant:'Large', qty:1, price:€12.00, modifications:'no onions'"
    },
    {
      "step": 6,
      "command": "Chat: \"I want pizza and pasta\" (ambiguous)",
      "expected": "AI asks clarifying questions: 'Which pizza? Which pasta? What sizes?' – does not output JSON until clarified"
    },
    {
      "step": 7,
      "command": "Chat: \"2x Burger, 1x Cola\" (food + drink)",
      "expected": "AI outputs JSON directly (no upsell prompt because drink already included)"
    },
    {
      "step": 8,
      "command": "Chat: \"2x Burger\" (food only)",
      "expected": "AI outputs JSON + appended upsell: '💡 Would you like to add a drink? We have Cola, Water, and fresh Lemonade.'"
    },
    {
      "step": 9,
      "command": "Simulate DeepSeek timeout (>25s)",
      "expected": "AbortController triggers, endpoint returns graceful fallback message, no crash, error logged at warn level"
    },
    {
      "step": 10,
      "command": "Security test: modify client payload to send price:0.01",
      "expected": "Server ignores client price, queries DB, uses actual variant price (e.g., €12.00) for priceAtTime"
    },
    {
      "step": 11,
      "command": "Run existing test suite: npm test",
      "expected": "All pre-existing tests pass – no regression from Prompt 29 changes"
    }
  ],
  
  "replit_specific_notes": [
    "DeepSeek API key must be set in Replit Secrets as DEEPSEEK_API_KEY",
    "Free tier has ~30s request timeout – the 25s AbortController provides safety margin",
    "Max 3 tool-call iterations prevents runaway API costs from ambiguous queries",
    "Idempotency keys table: add a cleanup cron job (run hourly) to delete keys older than 1 hour to control DB growth",
    "WebSocket pub/sub: reuse existing system – no new WS server code needed",
    "Drizzle transactions: ensure Neon connection supports transactions (it does)",
    "TypeScript: ensure tsconfig.json includes server/ directory for proper path resolution",
    "Logging: respect LOG_LEVEL – log DeepSeek calls and price validations at debug level only in production"
  ],
  
  "success_criteria": "Prompt 29 v2.0-Final is COMPLETE when ALL of the following are true:\n1. search_products tool returns variants JSONB array so AI can resolve size queries\n2. System prompt instructs AI to ask for variant clarification when not specified\n3. Intelligent upselling: if order has only food, AI appends drink suggestion before JSON\n4. OrderConfirmation component renders JSON block, generates idempotency UUID, sends to /confirm-order\n5. /confirm-order endpoint: validates prices from DB (never trusts client), enforces idempotency, inserts in transaction\n6. Admin dashboard displays order_items with variant names and server-validated prices\n7. WebSocket broadcast notifies admin in real-time after successful confirmation\n8. All 11 testing checkpoints pass successfully\n9. Zero TypeScript errors (npx tsc --noEmit)\n10. All pre-existing tests still pass (no regression)",
  
  "failure_handling": "If ANY deliverable cannot be implemented due to project constraints:\n1. STOP immediately and output: (a) the specific blocker, (b) which integration point is affected (Prompt 27/28/existing), (c) minimal viable alternative that maintains security and core UX\n2. DO NOT silently skip requirements or weaken security (e.g., removing idempotency or server-side price validation)\n3. Preserve already-implemented components – do not revert working code\n4. Wait for explicit user confirmation before proceeding with any workaround",
  
  "final_self_verification": "Before marking complete, run this checklist:\n□ Does search_products tool return variants JSONB array from Prompt 27?\n□ Does system prompt instruct AI to ask for variant clarification when not specified?\n□ Does AI append upsell suggestion when order contains only food (no drinks)?\n□ Does OrderConfirmation generate idempotency UUID and send with confirmation payload?\n□ Does /confirm-order validate prices from DB variants array – NEVER trust client price?\n□ Does /confirm-order check idempotency key and return {duplicate:true} if seen within 10 min?\n□ Are order_items inserted inside a Drizzle transaction for atomicity?\n□ Does admin dashboard display variantName and priceAtTime for each order item?\n□ Is WebSocket broadcast sent to admin channel after successful confirmation?\n□ Does npx tsc --noEmit return zero errors?\n□ Do all 11 testing checkpoints pass?\n□ Do all pre-existing tests still pass?\n\nIf ANY answer is NO: revert non-conforming changes, document the specific issue, and request clarification."
}