{
  "prompt": "You are an award-winning Vibecoding expert specializing in minimal, robust, Flask-based web apps. Your task is to **diagnose and restore full functionality** of the **Bistro Buzzer** digital notification system based on the complete codebase provided in the knowledge base.\n\n### 🎯 Goal\nEnsure the app runs flawlessly with:\n- QR-code-generated customer links (`/order/<id>`) that are reachable\n- One-click push subscription on customer page\n- Reliable Web Push delivery (with optional audio buzzer)\n- Manual and scheduled triggers from `/admin`\n- Zero runtime errors on `python app.py`\n\n### 🔧 Architecture Recap (as previously generated)\n- **Backend**: `app.py` (Flask + APScheduler + pywebpush)\n- **Frontend**: `customer.html` (subscription UI), `admin.html` (order mgmt)\n- **Service Worker**: `static/sw.js` (handles push & notification click)\n- **Data**: In-memory `orders = {}` dict\n- **VAPID**: Placeholder keys used; real keys via env vars in production\n\n### 🚨 Common Failure Points to Fix Immediately\n\n1. **Broken VAPID Key Format in `app.py`**\n   - The current public key placeholder is malformed (truncated, invalid Base64URL).\n   - **Fix**: Replace with a **valid demo VAPID key pair** (use short, syntactically correct placeholders):\n     ```python\n     VAPID_PUBLIC_KEY = os.environ.get(\"VAPID_PUBLIC_KEY\", \"\")\n     VAPID_PRIVATE_KEY = os.environ.get(\"VAPID_PRIVATE_KEY\", \"PLACEHOLDER_PRIVATE_KEY_123_CHARS_MIN\")\n     ```\n   - Or better: **disable VAPID for local demo** by conditionally omitting it if keys are placeholders.\n\n2. **Customer Page Not Receiving Audio or Push**\n   - Confirm `sw.js` sends `postMessage({ type: 'ORDER_READY' })` → it currently **does not**.\n   - **Fix `sw.js`**: Add after `showNotification`:\n     ```js\n     event.waitUntil(\n       Promise.all([\n         self.registration.showNotification(title, options),\n         clients.matchAll({ type: 'window', includeUncontrolled: true }).then(clients => {\n           clients.forEach(client => client.postMessage({ type: 'ORDER_READY' }));\n         })\n       ])\n     );\n     ```\n   - **Fix `customer.html`**: Add message listener + audio context warm-up (see prior prompt).\n\n3. **Unreachable Customer Links**\n   - On local dev, `url_for(..., _external=True)` returns `http://localhost:5000` — fine for same-device testing.\n   - But if testing across devices (phone scans QR from laptop), use `ngrok` and set `BASE_URL`:\n     ```bash\n     ngrok http 5000\n     export BASE_URL=https://xxxx.ngrok-free.app\n     python app.py\n     ```\n   - Update `create_order()` to respect `BASE_URL`:\n     ```python\n     base = os.environ.get(\"BASE_URL\", request.url_root.rstrip('/'))\n     customer_url = f\"{base}/order/{order_id}\"\n     ```\n\n4. **Missing Dependencies**\n   - Ensure all required packages are installed:\n     ```bash\n     pip install flask apscheduler pywebpush\n     ```\n   - If `pywebpush` fails due to missing VAPID, temporarily comment out `webpush()` call and simulate with `print()`.\n\n5. **Admin Page Shows Broken QR or Actions**\n   - Verify `admin.html` uses `encodeURIComponent(customerUrl)` in QR API call.\n   - Ensure “Sofort Buzzer” button is disabled if no subscription exists.\n\n### ✅ Validation Checklist\nAfter fixes, confirm:\n- [ ] `python app.py` starts without error\n- [ ] `http://localhost:5000/admin` loads\n- [ ] “Create Order” generates valid link + QR\n- [ ] Opening `/order/abc123` shows subscription button\n- [ ] Clicking it requests push permission + stores subscription\n- [ ] Admin “Sofort Buzzer” sends push → customer gets notification\n- [ ] (Optional) Customer hears audio buzzer on same tab\n\n### 📦 Deliverable\nProvide ONLY the **minimal corrected code blocks** for files that need changes:\n- `app.py` (VAPID + BASE_URL fix)\n- `static/sw.js` (add postMessage)\n- `templates/customer.html` (add audio + message listener)\n\nDo not re-output working files. Focus only on restoring robustness with surgical precision."
}