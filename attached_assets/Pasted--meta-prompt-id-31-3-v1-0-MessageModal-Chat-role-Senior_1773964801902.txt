{
  "meta": {
    "prompt_id": "31.3-v1.0-MessageModal-Chat",
    "role": "Senior Frontend Engineer",
    "purpose": "Transform the one-shot message modal into a persistent two-way conversation UI with quick replies.",
    "prerequisites": [
      "Prompt 31.1 is complete (urgency sorting + visual cues)",
      "MessageModal exists and can send messages to customer",
      "WebSocket sends/receives messages for order"
    ],
    "scope": "STRICTLY: Rewrite client/src/components/admin/MessageModal.tsx. NO backend changes, NO new API endpoints."
  },
  
  "critical_directive": "⚠️ SENIOR ENGINEER MODE: This is a REFACTOR prompt. The modal must keep existing functionality (send message) while adding chat history and quick replies. Do NOT change the message-sending API contract. Test that existing message flows still work after refactor.",
  
  "deliverables": [
    {
      "file": "client/src/components/admin/MessageModal.tsx",
      "task": "Rewrite as two-way chat UI with history, quick replies, and persistent open state",
      "code_requirements": [
        "import { ScrollArea } from '@/components/ui/scroll-area';",
        "import { Button } from '@/components/ui/button';",
        "import { Textarea } from '@/components/ui/textarea';",
        "",
        "export function MessageModal({",
        "  orderId,",
        "  isOpen,",
        "  onClose,",
        "  messages = [], // Array of { id, sender: 'customer'|'staff', text, timestamp, read }",
        "  onSendMessage,",
        "}: {",
        "  orderId: string;",
        "  isOpen: boolean;",
        "  onClose: () => void;",
        "  messages: Array<{ id: string; sender: 'customer' | 'staff'; text: string; timestamp: string; read?: boolean }>;",
        "  onSendMessage: (text: string) => Promise<void>;",
        "}) {",
        "  const [draft, setDraft] = useState('');",
        "  const scrollRef = useRef<HTMLDivElement>(null);",
        "  ",
        "  // Quick reply templates",
        "  const QUICK_REPLIES = [",
        "    'On its way! 🍽️',",
        "    'We are checking on that for you.',",
        "    'Please come to the counter to pick up your order.',",
        "    'Thank you for your patience!'",
        "  ];",
        "  ",
        "  // Auto-scroll to bottom when new message arrives",
        "  useEffect(() => {",
        "    if (scrollRef.current) {",
        "      scrollRef.current.scrollTop = scrollRef.current.scrollHeight;",
        "    }",
        "  }, [messages]);",
        "  ",
        "  const handleSend = async () => {",
        "    if (!draft.trim()) return;",
        "    ",
        "    // Optimistic update: append to local state immediately",
        "    const tempId = `temp-${Date.now()}`;",
        "    const newMessage = {",
        "      id: tempId,",
        "      sender: 'staff' as const,",
        "      text: draft.trim(),",
        "      timestamp: new Date().toISOString(),",
        "      read: true",
        "    };",
        "    ",
        "    // Keep modal open – do NOT call onClose()",
        "    await onSendMessage(draft.trim());",
        "    setDraft('');",
        "  };",
        "  ",
        "  if (!isOpen) return null;",
        "  ",
        "  return (",
        "    <div className=\"fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/50\" role=\"dialog\" aria-modal=\"true\">",
        "      <div className=\"bg-white rounded-2xl shadow-xl w-full max-w-2xl max-h-[90vh] flex flex-col\">",
        "        ",
        "        {/* Header */}",
        "        <div className=\"p-4 border-b flex justify-between items-center\">",
        "          <h3 className=\"font-semibold\">Chat – Order {orderId.slice(-4)}</h3>",
        "          <Button variant=\"ghost\" size=\"icon\" onClick={onClose}>",
        "            <X className=\"w-5 h-5\" />",
        "          </Button>",
        "        </div>",
        "        ",
        "        {/* Message History */}",
        "        <ScrollArea className=\"flex-1 p-4\" ref={scrollRef}>",
        "          <div className=\"space-y-4\">",
        "            {messages.map((msg) => (",
        "              <div",
        "                key={msg.id}",
        "                className={`flex ${msg.sender === 'staff' ? 'justify-end' : 'justify-start'}`}",
        "              >",
        "                <div",
        "                  className={`max-w-[80%] rounded-2xl px-4 py-2 ${",
        "                    msg.sender === 'staff'",
        "                      ? 'bg-blue-500 text-white rounded-br-none'",
        "                      : 'bg-gray-100 text-gray-900 rounded-bl-none'",
        "                  }`}",
        "                >",
        "                  <p className=\"text-sm whitespace-pre-wrap\">{msg.text}</p>",
        "                  <span className={`text-xs mt-1 block ${",
        "                    msg.sender === 'staff' ? 'text-blue-100' : 'text-gray-400'",
        "                  }`}>",
        "                    {new Date(msg.timestamp).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' })}",
        "                  </span>",
        "                </div>",
        "              </div>",
        "            ))}",
        "          </div>",
        "        </ScrollArea>",
        "        ",
        "        {/* Quick Replies */}",
        "        <div className=\"px-4 py-2 border-t bg-gray-50\">",
        "          <div className=\"flex flex-wrap gap-2\">",
        "            {QUICK_REPLIES.map((reply) => (",
        "              <Button",
        "                key={reply}",
        "                variant=\"outline\"",
        "                size=\"sm\"",
        "                className=\"text-xs\"",
        "                onClick={() => {",
        "                  setDraft(reply);",
        "                  // Optional: auto-send after short delay",
        "                  setTimeout(() => handleSend(), 100);",
        "                }}",
        "              >",
        "                {reply}",
        "              </Button>",
        "            ))}",
        "          </div>",
        "        </div>",
        "        ",
        "        {/* Compose Area */}",
        "        <div className=\"p-4 border-t flex gap-2\">",
        "          <Textarea",
        "            value={draft}",
        "            onChange={(e) => setDraft(e.target.value)}",
        "            placeholder=\"Type a message...\"",
        "            className=\"flex-1 resize-none\"",
        "            rows={2}",
        "            onKeyDown={(e) => {",
        "              if (e.key === 'Enter' && !e.shiftKey) {",
        "                e.preventDefault();",
        "                handleSend();",
        "              }",
        "            }}",
        "          />",
        "          <Button onClick={handleSend} disabled={!draft.trim()}>",
        "            Send",
        "          </Button>",
        "        </div>",
        "      </div>",
        "    </div>",
        "  );",
        "}",
        "",
        "// Accessibility notes:",
        "// - role=\"dialog\" and aria-modal=\"true\" for screen readers",
        "// - Focus trap: use focus-trap-react if available, or manage focus manually",
        "// - Escape key closes modal: add onKeyDown handler at root div"
      ]
    },
    {
      "file": "client/src/pages/admin.tsx",
      "task": "Update MessageModal integration to pass full message history",
      "code_requirements": [
        "// 1. Ensure message history is fetched for the order",
        "// If using React Query:",
        "const {  orderMessages } = useQuery({",
        "  queryKey: ['order-messages', orderId],",
        "  queryFn: () => fetch(`/api/orders/${orderId}/messages`).then(r => r.json())",
        "});",
        "",
        "// 2. Pass messages to MessageModal",
        "<MessageModal",
        "  orderId={selectedOrderId}",
        "  isOpen={!!selectedOrderId}",
        "  onClose={() => setSelectedOrderId(null)}",
        "  messages={orderMessages || []}",
        "  onSendMessage={async (text) => {",
        "    // Existing send logic – unchanged API contract",
        "    await fetch(`/api/orders/${selectedOrderId}/message`, {",
        "      method: 'POST',",
        "      headers: { 'Content-Type': 'application/json' },",
        "      body: JSON.stringify({ text })",
        "    });",
        "    // Invalidate query to refetch history",
        "    queryClient.invalidateQueries({ queryKey: ['order-messages', selectedOrderId] });",
        "  }}",
        "/>",
        "",
        "// 3. When modal opens, mark customer messages as read (for urgency sorting)",
        "useEffect(() => {",
        "  if (selectedOrderId) {",
        "    // Update local order state to mark messages as read",
        "    setOrders(prev => prev.map(order =>",
        "      order.id === selectedOrderId",
        "        ? { ...order, hasUnreadMessage: false }",
        "        : order",
        "    ));",
        "  }",
        "}, [selectedOrderId]);"
      ]
    }
  ],
  
  "testing_checkpoints": [
    {
      "step": 1,
      "command": "Open admin dashboard, click on order with existing messages",
      "expected": "MessageModal opens showing full chat history. Customer messages left-aligned (gray), staff messages right-aligned (blue)."
    },
    {
      "step": 2,
      "command": "Type message and press Enter (without Shift)",
      "expected": "Message sends, appears in chat history immediately (optimistic update), modal STAYS OPEN."
    },
    {
      "step": 3,
      "command": "Click a Quick Reply chip",
      "expected": "Chip text appears in textarea and sends automatically within 100ms. Modal stays open."
    },
    {
      "step": 4,
      "command": "Receive new customer message while modal is open",
      "expected": "New message appears in chat history, auto-scrolls to bottom, unread indicator on OrderCard disappears."
    },
    {
      "step": 5,
      "command": "Run npm test",
      "expected": "All pre-existing tests pass; message-sending API contract unchanged."
    }
  ],
  
  "replit_specific_notes": [
    "If shadcn/ui ScrollArea is not installed: use a simple div with overflow-y-auto and custom scrollbar styling.",
    "Optimistic update: append message to local state before API response for snappy UX. Handle rollback if API fails.",
    "Auto-scroll: use ref + scrollHeight to ensure newest message is visible. Test with long message history.",
    "Keyboard shortcuts: Enter sends, Shift+Enter adds newline – standard chat behavior."
  ],
  
  "success_criteria": "Prompt 31.3 is COMPLETE when:\n1. MessageModal shows full two-way chat history (not just compose box)\n2. Customer/staff messages have distinct visual styles (left/gray vs right/blue)\n3. Modal stays open after sending message (no auto-close)\n4. Quick Reply chips send messages instantly with one click\n5. Chat auto-scrolls to newest message\n6. Unread indicator on OrderCard clears when modal opens\n7. All 5 testing checkpoints pass\n8. Zero TypeScript errors (npx tsc --noEmit)\n9. All pre-existing tests still pass (message API contract unchanged)",
  
  "failure_handling": "If modal doesn't stay open after send:\n1. Verify onClose() is NOT called in handleSend function\n2. Check that optimistic update appends to local state before API call\n3. If ScrollArea fails: fallback to div with overflow-y-auto\n4. Do NOT revert to one-shot modal – two-way chat is critical for staff UX\n5. Wait for explicit confirmation before proceeding to Prompt 31.4",
  
  "final_self_verification": "Before marking complete:\n□ Does MessageModal show full chat history, not just compose box?\n□ Are customer messages left-aligned (gray) and staff messages right-aligned (blue)?\n□ Does modal stay open after sending a message?\n□ Do Quick Reply chips send messages with one click?\n□ Does chat auto-scroll to newest message?\n□ Does unread indicator clear when modal opens?\n□ Is the message-sending API contract unchanged (backward compatible)?\n□ Does npx tsc --noEmit return 0 errors?\n□ Do all pre-existing tests still pass?\n\nIf ANY answer is NO: fix before proceeding."
}