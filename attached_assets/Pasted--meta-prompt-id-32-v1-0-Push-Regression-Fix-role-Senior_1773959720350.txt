{
  "meta": {
    "prompt_id": "32-v1.0-Push-Regression-Fix",
    "role": "Senior Fullstack Engineer",
    "purpose": "Restore Web Push notification functionality that regressed during recent refactors. Push notifications were working in earlier versions (Prompts 13-14) but are now broken or missing.",
    "prerequisites": [
      "Prompts 1-31 are implemented, including DbStorage migration (Prompt 30.6)",
      "VAPID keys exist in Replit Secrets: VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY",
      "Service Worker is registered (from Prompt 13/14) but push events may not fire"
    ],
    "scope": "STRICTLY: Restore Web Push flow – permission request, subscription management, backend send endpoint, service worker handling. DO NOT modify unrelated audio/chat/ordering logic."
  },
  
  "critical_directive": "⚠️ SENIOR ENGINEER MODE: This is a REGRESSION FIX, not a new feature. Push notifications worked before – your job is to identify what broke and restore the working flow. Do NOT rewrite from scratch; recover the proven implementation pattern from Prompts 13-14. Test end-to-end: permission → subscribe → trigger → receive.",
  
  "project_context": {
    "name": "Bistro Buzzer",
    "platform": "Replit (Vite PWA, Node.js/Express, PostgreSQL/Neon, Web Push API)",
    "push_architecture": {
      "frontend": "Service Worker (public/sw.js) + frontend subscription logic",
      "backend": "web-push library to send notifications via browser push services",
      "trigger": "Order status change to 'notified' or 'ready' broadcasts push to customer",
      "fallback": "If push fails, log warning; WebSocket already handles real-time fallback"
    },
    "known_working_state": "Before Prompt 27-31 refactors, push worked: user granted permission → subscription stored → order ready → push received on mobile/desktop."
  },
  
  "objective": "Restore full Web Push notification flow: (1) user permission request on interaction, (2) subscription storage in PostgreSQL, (3) backend send endpoint triggered by order status changes, (4) service worker displays notification with sound/vibration, (5) end-to-end test validates delivery.",
  
  "deliverables": [
    {
      "file": "server/lib/push-service.ts",
      "task": "Re-implement push sending service with web-push library",
      "code_requirements": [
        "import webpush from 'web-push';",
        "",
        "// Initialize with VAPID keys from env",
        "webpush.setVapidDetails(",
        "  'mailto:admin@bistrobuzzer.app',",
        "  process.env.VAPID_PUBLIC_KEY!,",
        "  process.env.VAPID_PRIVATE_KEY!",
        ");",
        "",
        "export async function sendPushNotification(",
        "  subscription: PushSubscriptionJSON,",
        "  payload: { title: string; body: string; icon?: string; data?: any }",
        "): Promise<boolean> {",
        "  try {",
        "    await webpush.sendNotification(",
        "      subscription as webpush.PushSubscription,",
        "      JSON.stringify({",
        "        title: payload.title,",
        "        body: payload.body,",
        "        icon: payload.icon || '/icons/icon-192.png',",
        "        data: {",
        "          ...payload.data,",
        "          timestamp: Date.now(),",
        "          // Replit-specific: ensure notification opens correct order page",
        "          url: `/order/${payload.data?.orderId}`",
        "        }",
        "      })",
        "    );",
        "    console.log(`[Push] Sent to ${subscription.endpoint?.slice(0, 30)}...`);",
        "    return true;",
        "  } catch (err: any) {",
        "    // Handle common errors gracefully",
        "    if (err.statusCode === 410) {",
        "      // Subscription expired/revoked – mark for cleanup",
        "      console.warn(`[Push] Subscription expired (410): ${subscription.endpoint}`);",
        "      return false;",
        "    }",
        "    if (err.statusCode === 401) {",
        "      // Auth error – likely invalid VAPID keys",
        "      console.error(`[Push] Auth failed (401): Check VAPID keys in Secrets`);",
        "      return false;",
        "    }",
        "    console.warn(`[Push] Send failed: ${err.message}`);",
        "    return false;",
        "  }",
        "}",
        "",
        "// Helper: validate subscription before storing",
        "export function isValidPushSubscription(sub: PushSubscriptionJSON): boolean {",
        "  return !!(sub.endpoint && sub.keys?.p256dh && sub.keys?.auth);",
        "}"
      ]
    },
    {
      "file": "server/routes/push.ts",
      "task": "Add endpoints for subscription management and sending",
      "code_requirements": [
        "import { Router } from 'express';",
        "import { sendPushNotification, isValidPushSubscription } from '@/server/lib/push-service';",
        "import { pushSubscriptions } from '@/shared/db-schema';",
        "import { eq, and } from 'drizzle-orm';",
        "",
        "const router = Router();",
        "",
        "// POST /api/push/subscribe – store new subscription",
        "router.post('/api/push/subscribe', async (req, res) => {",
        "  const { orderId, subscription } = req.body;",
        "  ",
        "  if (!orderId || !isValidPushSubscription(subscription)) {",
        "    return res.status(400).json({ error: 'Invalid subscription data' });",
        "  }",
        "  ",
        "  // Upsert: one subscription per order (simplifies cleanup)",
        "  await db.insert(pushSubscriptions)",
        "    .values({",
        "      orderId,",
        "      endpoint: subscription.endpoint!,",
        "      p256dh: subscription.keys!.p256dh,",
        "      auth: subscription.keys!.auth,",
        "      userAgent: req.headers['user-agent'],",
        "      subscribedAt: new Date()",
        "    })",
        "    .onConflictDoUpdate({",
        "      target: pushSubscriptions.orderId,",
        "      set: {",
        "        endpoint: subscription.endpoint!,",
        "        p256dh: subscription.keys!.p256dh,",
        "        auth: subscription.keys!.auth,",
        "        subscribedAt: new Date()",
        "      }",
        "    });",
        "  ",
        "  res.json({ success: true });",
        "});",
        "",
        "// POST /api/push/unsubscribe – cleanup (optional, browser handles revocation)",
        "router.post('/api/push/unsubscribe', async (req, res) => {",
        "  const { orderId } = req.body;",
        "  if (!orderId) return res.status(400).json({ error: 'orderId required' });",
        "  ",
        "  await db.delete(pushSubscriptions).where(eq(pushSubscriptions.orderId, orderId));",
        "  res.json({ success: true });",
        "});",
        "",
        "// POST /api/push/send – internal endpoint triggered by order status change",
        "router.post('/api/push/send', async (req, res) => {",
        "  const { orderId, title, body, icon, data } = req.body;",
        "  ",
        "  // Fetch subscription for this order",
        "  const sub = await db.query.pushSubscriptions.findFirst({",
        "    where: eq(pushSubscriptions.orderId, orderId)",
        "  });",
        "  ",
        "  if (!sub) {",
        "    return res.status(404).json({ error: 'No subscription found for order' });",
        "  }",
        "  ",
        "  const subscription: PushSubscriptionJSON = {",
        "    endpoint: sub.endpoint,",
        "    keys: { p256dh: sub.p256dh, auth: sub.auth }",
        "  };",
        "  ",
        "  const sent = await sendPushNotification(subscription, { title, body, icon, data });",
        "  ",
        "  if (!sent) {",
        "    // Optional: mark subscription for cleanup if 410 error",
        "    return res.status(502).json({ error: 'Failed to send push' });",
        "  }",
        "  ",
        "  res.json({ success: true });",
        "});",
        "",
        "export default router;"
      ]
    },
    {
      "file": "public/sw.js",
      "task": "Restore service worker push event handler with notification display",
      "code_requirements": [
        "// Add to existing service worker (do not replace other handlers)",
        "",
        "self.addEventListener('push', (event) => {",
        "  if (!event.data) return;",
        "  ",
        "  const payload = event.data.json();",
        "  const { title, body, icon, data } = payload;",
        "  ",
        "  event.waitUntil(",
        "    self.registration.showNotification(title, {",
        "      body,",
        "      icon: icon || '/icons/icon-192.png',",
        "      badge: '/icons/badge-72.png',",
        "      tag: data?.orderId ? `order-${data.orderId}` : undefined,",
        "      renotify: true,",
        "      requireInteraction: true, // Keep visible until user acts",
        "      data: {",
        "        ...data,",
        "        // Replit PWA: ensure click opens correct order in same window",
        "        url: data?.url || '/',",
        "        timestamp: Date.now()",
        "      }",
        "    })",
        "  );",
        "});",
        "",
        "// Handle notification click – open order page",
        "self.addEventListener('notificationclick', (event) => {",
        "  event.notification.close();",
        "  ",
        "  const url = event.notification.data?.url || '/';",
        "  ",
        "  event.waitUntil(",
        "    clients.matchAll({ type: 'window', includeUncontrolled: true }).then((windowClients) => {",
        "      // Try to focus existing order page tab",
        "      const orderClient = windowClients.find((c) => c.url.includes('/order/'));",
        "      if (orderClient && 'focus' in orderClient) {",
        "        return orderClient.focus();",
        "      }",
        "      // Otherwise open new tab",
        "      if (clients.openWindow) {",
        "        return clients.openWindow(url);",
        "      }",
        "    })",
        "  );",
        "});",
        "",
        "// Optional: handle notification close for analytics",
        "self.addEventListener('notificationclose', (event) => {",
        "  // Could log dismissal for UX insights",
        "});"
      ]
    },
    {
      "file": "client/src/lib/push-manager.ts",
      "task": "Frontend subscription logic with interaction-gated permission flow",
      "code_requirements": [
        "export class PushManager {",
        "  private vapidPublicKey: string;",
        "  private registration: ServiceWorkerRegistration | null = null;",
        "  ",
        "  constructor(vapidPublicKey: string) {",
        "    this.vapidPublicKey = vapidPublicKey;",
        "  }",
        "  ",
        "  // CRITICAL: Only request permission after user interaction (iOS Safari requirement)",
        "  async requestPermissionOnInteraction(): Promise<NotificationPermission> {",
        "    if (!('Notification' in window)) {",
        "      console.warn('[Push] Notifications not supported');",
        "      return 'denied';",
        "    }",
        "    ",
        "    // If already granted, just subscribe",
        "    if (Notification.permission === 'granted') {",
        "      return this.subscribe();",
        "    }",
        "    ",
        "    // Request permission – must be triggered by user gesture",
        "    const permission = await Notification.requestPermission();",
        "    if (permission === 'granted') {",
        "      return this.subscribe();",
        "    }",
        "    return permission;",
        "  }",
        "  ",
        "  async subscribe(): Promise<NotificationPermission> {",
        "    try {",
        "      // Get or register service worker",
        "      if (!this.registration) {",
        "        this.registration = await navigator.serviceWorker.ready;",
        "      }",
        "      ",
        "      // Check existing subscription",
        "      let subscription = await this.registration.pushManager.getSubscription();",
        "      ",
        "      if (!subscription) {",
        "        // Subscribe with VAPID key",
        "        subscription = await this.registration.pushManager.subscribe({",
        "          userVisibleOnly: true,",
        "          applicationServerKey: this.urlBase64ToUint8Array(this.vapidPublicKey)",
        "        });",
        "      }",
        "      ",
        "      // Send subscription to backend",
        "      const response = await fetch('/api/push/subscribe', {",
        "        method: 'POST',",
        "        headers: { 'Content-Type': 'application/json' },",
        "        body: JSON.stringify({",
        "          orderId: this.getCurrentOrderId(), // Implement based on your routing",
        "          subscription: subscription.toJSON()",
        "        })",
        "      });",
        "      ",
        "      if (!response.ok) {",
        "        throw new Error('Failed to register subscription');",
        "      }",
        "      ",
        "      console.log('[Push] Subscribed successfully');",
        "      return 'granted';",
        "    } catch (err) {",
        "      console.error('[Push] Subscribe failed:', err);",
        "      return 'denied';",
        "    }",
        "  }",
        "  ",
        "  // Helper: Convert VAPID key for pushManager.subscribe",
        "  private urlBase64ToUint8Array(base64String: string): Uint8Array {",
        "    const padding = '='.repeat((4 - (base64String.length % 4)) % 4);",
        "    const base64 = (base64String + padding).replace(/-/g, '+').replace(/_/g, '/');",
        "    const rawData = atob(base64);",
        "    const outputArray = new Uint8Array(rawData.length);",
        "    for (let i = 0; i < rawData.length; ++i) {",
        "      outputArray[i] = rawData.charCodeAt(i);",
        "    }",
        "    return outputArray;",
        "  }",
        "  ",
        "  private getCurrentOrderId(): string | null {",
        "    // Extract from URL: /order/ABC-123 → ABC-123",
        "    const match = window.location.pathname.match(/\\/order\\/([^/]+)/);",
        "    return match?.[1] || null;",
        "  }",
        "}"
      ]
    },
    {
      "file": "client/src/components/guest-assistant.tsx",
      "task": "Integrate push permission request on user interaction (not on load)",
      "code_requirements": [
        "// Import PushManager",
        "import { PushManager } from '@/lib/push-manager';",
        "",
        "// Initialize once (e.g., in useEffect at component mount)",
        "useEffect(() => {",
        "  if (process.env.VITE_VAPID_PUBLIC_KEY) {",
        "    const pushMgr = new PushManager(process.env.VITE_VAPID_PUBLIC_KEY);",
        "    window.pushManager = pushMgr; // Optional: global access for debugging",
        "  }",
        "}, []);",
        "",
        "// CRITICAL: Request permission ONLY after user interaction",
        "// Add to existing interaction handler (click/touch/scroll) that already unlocks audio:",
        "const handleUserInteraction = async () => {",
        "  // Existing: resume audio context",
        "  await audioManager.resume();",
        "  ",
        "  // NEW: Request push permission (idempotent – only prompts if not decided)",
        "  if (window.pushManager && Notification.permission === 'default') {",
        "    // Debounce to avoid multiple prompts on rapid taps",
        "    if (!window.pushPermissionRequested) {",
        "      window.pushPermissionRequested = true;",
        "      try {",
        "        await window.pushManager.requestPermissionOnInteraction();",
        "      } catch (err) {",
        "        console.warn('[Push] Permission request failed:', err);",
        "      }",
        "    }",
        "  }",
        "};",
        "",
        "// Attach to existing interaction listeners:",
        "useEffect(() => {",
        "  const events = ['click', 'touchstart', 'keydown', 'scroll'];",
        "  const handler = () => handleUserInteraction();",
        "  ",
        "  events.forEach((evt) => window.addEventListener(evt, handler, { passive: true, once: true }));",
        "  ",
        "  return () => {",
        "    events.forEach((evt) => window.removeEventListener(evt, handler));",
        "  };",
        "}, []);",
        "",
        "// Optional: UI hint if permission not granted",
        "{Notification.permission === 'denied' && (",
        "  <p className=\"text-xs text-gray-500 mt-2\">",
        "    🔔 Push notifications blocked. Enable in browser settings for order alerts.",
        "  </p>",
        ")}"
      ]
    },
    {
      "file": "server/lib/order-notifier.ts",
      "task": "Trigger push when order status changes to 'ready' or 'notified'",
      "code_requirements": [
        "// Add to existing order status update logic",
        "export async function notifyOrderReady(orderId: string, db: NeonQueryFn) {",
        "  // 1. Fetch order details for notification content",
        "  const order = await db.query.orders.findFirst({",
        "    where: eq(orders.id, orderId),",
        "    with: { items: true } // If you have order_items relation",
        "  });",
        "  if (!order) return;",
        "  ",
        "  // 2. Prepare notification payload",
        "  const payload = {",
        "    title: '🍽️ Your order is ready!',",
        "    body: `Order ${orderId} – please pick up at the counter.`,",
        "    icon: '/icons/icon-192.png',",
        "    data: {",
        "      orderId,",
        "      status: order.status,",
        "      url: `/order/${orderId}`",
        "    }",
        "  };",
        "  ",
        "  // 3. Send push via internal endpoint (or call sendPushNotification directly)",
        "  try {",
        "    const response = await fetch(`${process.env.APP_URL}/api/push/send`, {",
        "      method: 'POST',",
        "      headers: { 'Content-Type': 'application/json' },",
        "      body: JSON.stringify({ orderId, ...payload })",
        "    });",
        "    ",
        "    if (!response.ok) {",
        "      const error = await response.json();",
        "      console.warn(`[Push] Send failed for order ${orderId}:`, error);",
        "      // Fallback: WebSocket already broadcasts status – no extra action needed",
        "    }",
        "  } catch (err) {",
        "    console.error(`[Push] Network error sending notification:`, err);",
        "    // Fallback to WebSocket (already implemented)",
        "  }",
        "  ",
        "  // 4. Broadcast via WebSocket as primary real-time channel",
        "  await publishToChannel(`order:${orderId}`, {",
        "    type: 'ORDER_READY',",
        "    timestamp: new Date().toISOString()",
        "  });",
        "}"
      ]
    },
    {
      "file": "shared/db-schema.ts",
      "task": "Add push_subscriptions table if not already present",
      "code_requirements": [
        "// Add to existing schema exports",
        "export const pushSubscriptions = pgTable('push_subscriptions', {",
        "  id: serial('id').primaryKey(),",
        "  orderId: text('order_id').notNull().unique(), // One subscription per order",
        "  endpoint: text('endpoint').notNull(),",
        "  p256dh: text('p256dh').notNull(),",
        "  auth: text('auth').notNull(),",
        "  userAgent: text('user_agent'),",
        "  subscribedAt: timestamp('subscribed_at').defaultNow(),",
        "  lastUsed: timestamp('last_used')",
        "});",
        "",
        "// Index for cleanup queries",
        "export const pushSubscriptionsOrderIdx = index('push_sub_order_idx').on(pushSubscriptions.orderId);"
      ]
    },
    {
      "file": "package.json",
      "task": "Ensure web-push dependency is installed",
      "code_requirements": [
        "// Add to dependencies if missing:",
        "\"web-push\": \"^3.6.7\"",
        "",
        "// Then run: npm install"
      ]
    },
    {
      "file": ".env.example",
      "task": "Document required VAPID keys",
      "content": [
        "# Web Push Notifications",
        "VAPID_PUBLIC_KEY=your-vapid-public-key-here",
        "VAPID_PRIVATE_KEY=your-vapid-private-key-here",
        "",
        "# Generate keys with:",
        "# npx web-push generate-vapid-keys --json"
      ]
    }
  ],
  
  "testing_checkpoints": [
    {
      "step": 1,
      "command": "Verify VAPID keys in Replit Secrets",
      "expected": "VAPID_PUBLIC_KEY and VAPID_PRIVATE_KEY are set (not empty)"
    },
    {
      "step": 2,
      "command": "Open customer order page in Chrome DevTools → Application → Push Messaging",
      "expected": "No errors in console; Service Worker registered; Push Manager shows subscription status"
    },
    {
      "step": 3,
      "command": "Interact with page (click/tap) to trigger permission request",
      "expected": "Browser shows native permission prompt; after 'Allow', subscription stored in DB"
    },
    {
      "step": 4,
      "command": "Check database: SELECT * FROM push_subscriptions WHERE order_id = 'test-order'",
      "expected": "Row exists with endpoint, p256dh, auth values (not null)"
    },
    {
      "step": 5,
      "command": "Manually trigger push via API: curl -X POST /api/push/send with test payload",
      "expected": "Response 200; notification appears on device within 5 seconds"
    },
    {
      "step": 6,
      "command": "Change order status to 'ready' via admin dashboard or API",
      "expected": "Push notification received with title '🍽️ Your order is ready!' and correct order ID"
    },
    {
      "step": 7,
      "command": "Click notification",
      "expected": "Browser opens/focuses the correct order page (/order/ABC-123)"
    },
    {
      "step": 8,
      "command": "Revoke notification permission in browser settings, reload page, interact",
      "expected": "Permission prompt reappears; if denied, UI shows helpful hint (no console errors)"
    },
    {
      "step": 9,
      "command": "Run npm test",
      "expected": "All 103 existing tests still pass; no regression from push restoration"
    },
    {
      "step": 10,
      "command": "Test on iOS Safari (PWA installed)",
      "expected": "Permission request works after interaction; notification received when app is backgrounded"
    }
  ],
  
  "replit_specific_notes": [
    "Replit PWA requires HTTPS – previews use https://*.replit.app, so push works in production preview",
    "VAPID keys must be generated once and stored in Secrets – do NOT regenerate per deploy",
    "Service Worker caching: after sw.js changes, force refresh with Ctrl+Shift+R or unregister in DevTools",
    "iOS Safari: push only works if PWA is installed to home screen AND user granted permission",
    "Fallback: WebSocket real-time updates already work – push is enhancement for backgrounded apps",
    "Debugging: add console.log in sw.js push event; view logs via chrome://serviceworker-internals"
  ],
  
  "success_criteria": "Prompt 32 is COMPLETE when:\n1. VAPID keys configured in Replit Secrets\n2. push_subscriptions table exists with proper schema\n3. Frontend requests permission ONLY after user interaction (not on load)\n4. Subscription is stored in PostgreSQL and retrievable by orderId\n5. Backend send endpoint works with web-push library\n6. Service worker displays notification with correct title/body/icon\n7. Notification click opens correct order page\n8. Order status change to 'ready' triggers push automatically\n9. All 10 testing checkpoints pass\n10. All 103 pre-existing tests still pass (no regression)",
  
  "failure_handling": "If push fails to work:\n1. Check browser console for 'Push Manager' errors – common: missing VAPID key, HTTP instead of HTTPS\n2. Verify service worker is active: chrome://serviceworker-internals → look for your scope\n3. Test with simple payload first: {title:'Test',body:'Hello'} before complex order data\n4. If iOS fails: ensure PWA is installed to home screen (not just visited in Safari)\n5. DO NOT disable push entirely – fallback to WebSocket is already implemented; push is enhancement\n6. If VAPID key errors: regenerate keys with `npx web-push generate-vapid-keys --json` and update Secrets",
  
  "final_self_verification": "Before marking complete, run this checklist:\n□ Are VAPID_PUBLIC_KEY and VAPID_PRIVATE_KEY set in Replit Secrets?\n□ Does push_subscriptions table exist with endpoint/p256dh/auth columns?\n□ Does frontend request permission ONLY after user interaction (click/tap), not on page load?\n□ Is subscription sent to /api/push/subscribe and stored in PostgreSQL?\n□ Does POST /api/push/send successfully call web-push.sendNotification?\n□ Does sw.js push event show notification with title, body, icon, and click handler?\n□ Does clicking notification open the correct order page (/order/ABC-123)?\n□ Does changing order status to 'ready' trigger push automatically?\n□ Do all 10 testing checkpoints pass (including iOS Safari if possible)?\n□ Do all 103 pre-existing tests still pass (npm test)?\n\nIf ANY answer is NO: debug the specific failure before marking complete. Push is a user-facing feature – it must work reliably."
}