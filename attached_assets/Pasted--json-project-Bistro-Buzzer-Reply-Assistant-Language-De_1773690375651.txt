```json
{
  "project": "Bistro Buzzer – Reply Assistant Language Detection Improvement",
  "version": "1.2",
  "description": "Improve the language detection of the Reply Assistant by considering the last customer message in full length and using a stronger, explicit language hint in the prompt. Optionally, a simple language detection library (e.g., 'iso-639-1' or 'franc') can be integrated to specify the target language precisely.",
  "instructions": [
    {
      "step": 1,
      "title": "Option A: Improved prompt without external library",
      "details": "Modify the prompt so that the last customer message is passed unshortened (or with a higher limit) and add a stronger instruction to respond in the same language. The message history is still used for context, but the last message receives special attention.",
      "files": [
        {
          "path": "server/lib/deepseek.ts",
          "action": "modify",
          "description": "Adapt the `getReplySuggestion` function: use the last customer message in full length (not truncated) and add a clear language hint.",
          "content": "import axios from 'axios';\nimport { createHash } from 'crypto';\nimport { logger } from './logger';\n\nconst DEEPSEEK_API_URL = 'https://api.deepseek.com/v1/chat/completions';\nconst CACHE_TTL_MS = 60 * 60 * 1000; // 1 hour\nconst MAX_MESSAGES = 5;               // Use only the last 5 messages for context\nconst MAX_MESSAGE_LENGTH = 100;        // Truncate older messages to save tokens\nconst MAX_LAST_CUSTOMER_LENGTH = 500;  // Keep last customer message full length\n\ninterface CacheEntry {\n  suggestion: string;\n  timestamp: number;\n}\n\nconst suggestionCache = new Map<string, CacheEntry>();\n\nfunction generateCacheKey(messages: any[], orderStatus: string): string {\n  // For the cache key we continue to use the truncated versions (consistency)\n  const relevantData = messages\n    .map(m => `${m.sender}:${m.text.substring(0, MAX_MESSAGE_LENGTH)}`)\n    .join('|');\n  const hash = createHash('sha256').update(relevantData + orderStatus).digest('hex');\n  return hash;\n}\n\nfunction getCachedSuggestion(key: string): string | null {\n  const entry = suggestionCache.get(key);\n  if (entry && Date.now() - entry.timestamp < CACHE_TTL_MS) {\n    return entry.suggestion;\n  }\n  suggestionCache.delete(key);\n  return null;\n}\n\nfunction setCachedSuggestion(key: string, suggestion: string): void {\n  if (suggestionCache.size > 100) {\n    const oldestKey = suggestionCache.keys().next().value;\n    suggestionCache.delete(oldestKey);\n  }\n  suggestionCache.set(key, { suggestion, timestamp: Date.now() });\n}\n\ninterface DeepSeekMessage {\n  role: 'system' | 'user' | 'assistant';\n  content: string;\n}\n\nexport async function callDeepSeek(\n  messages: DeepSeekMessage[],\n  options?: { maxTokens?: number; temperature?: number }\n): Promise<string> {\n  if (!process.env.DEEPSEEK_API_KEY) {\n    throw new Error('DEEPSEEK_API_KEY not configured');\n  }\n\n  try {\n    const response = await axios.post(\n      DEEPSEEK_API_URL,\n      {\n        model: 'deepseek-chat',\n        messages,\n        max_tokens: options?.maxTokens ?? 150,\n        temperature: options?.temperature ?? 0.7,\n      },\n      {\n        headers: {\n          'Authorization': `Bearer ${process.env.DEEPSEEK_API_KEY}`,\n          'Content-Type': 'application/json',\n        },\n        timeout: 8000,\n      }\n    );\n    return response.data.choices[0].message.content;\n  } catch (error) {\n    logger.error({ error }, 'DeepSeek API call failed');\n    throw error;\n  }\n}\n\nexport async function getReplySuggestion(\n  messageHistory: any[],\n  orderStatus: string\n): Promise<string> {\n  // 1. Reduce messages and truncate older ones\n  const recentMessages = messageHistory.slice(-MAX_MESSAGES).map(m => ({\n    sender: m.sender,\n    text: m.text.length > MAX_MESSAGE_LENGTH ? m.text.substring(0, MAX_MESSAGE_LENGTH) + '…' : m.text,\n    fullText: m.text, // full text for the last customer message\n  }));\n\n  // 2. Identify the last customer message (in full length)\n  const lastCustomerMsgObj = messageHistory\n    .filter(m => m.sender === 'customer')\n    .pop();\n  const lastCustomerFullText = lastCustomerMsgObj\n    ? (lastCustomerMsgObj.text.length > MAX_LAST_CUSTOMER_LENGTH \n        ? lastCustomerMsgObj.text.substring(0, MAX_LAST_CUSTOMER_LENGTH) + '…' \n        : lastCustomerMsgObj.text)\n    : null;\n\n  // 3. Generate cache key (using truncated data)\n  const cacheKey = generateCacheKey(recentMessages, orderStatus);\n  const cached = getCachedSuggestion(cacheKey);\n  if (cached) {\n    logger.debug('Returning cached suggestion');\n    return cached;\n  }\n\n  // 4. Build prompt – with strong language hint\n  const formatted = recentMessages\n    .map(m => `${m.sender === 'customer' ? 'Customer' : 'Staff'}: ${m.text}`)\n    .join('\\n');\n\n  let languageInstruction = 'Respond in a friendly, professional tone.';\n  if (lastCustomerFullText) {\n    languageInstruction = `IMPORTANT: The customer's last message is: \"${lastCustomerFullText}\". You MUST respond in the EXACT SAME LANGUAGE as that message. Do not switch languages.`;\n  }\n\n  const systemPrompt = `You are a helpful assistant for restaurant staff. \nBased on the conversation history and the current order status (${orderStatus}), generate a short, friendly reply to the customer. \nThe reply should be at most 200 characters and match the tone of the conversation.\n${languageInstruction}`;\n\n  const userPrompt = `Message history:\n${formatted}\n\nReply suggestion:`;\n\n  // 5. Call API\n  try {\n    const suggestion = await callDeepSeek([\n      { role: 'system', content: systemPrompt },\n      { role: 'user', content: userPrompt },\n    ], { maxTokens: 100, temperature: 0.5 });\n\n    const trimmed = suggestion.length > 200 ? suggestion.substring(0, 200) + '…' : suggestion;\n\n    setCachedSuggestion(cacheKey, trimmed);\n    return trimmed;\n  } catch (error) {\n    throw error;\n  }\n}\n"
        }
      ]
    },
    {
      "step": 2,
      "title": "Option B: Explicit language detection with a small library (recommended for higher accuracy)",
      "details": "Install a lightweight language detection library such as 'iso-639-1' or 'franc-min' to determine the language of the last customer message. The detected language is then directly mentioned in the prompt.",
      "dependencies": ["franc-min@^6.0.0", "iso-639-1@^2.1.15"],
      "command": "npm install franc-min iso-639-1",
      "files": [
        {
          "path": "server/lib/deepseek.ts",
          "action": "modify",
          "description": "Extend the file with language detection using 'franc-min' and 'iso-639-1'. The detected language code (e.g., 'eng', 'deu') is converted to a readable name (e.g., 'English', 'German') and used in the prompt.",
          "content": "import axios from 'axios';\nimport { createHash } from 'crypto';\nimport { logger } from './logger';\nimport { franc } from 'franc-min';\nimport iso6391 from 'iso-639-1';\n\nconst DEEPSEEK_API_URL = 'https://api.deepseek.com/v1/chat/completions';\nconst CACHE_TTL_MS = 60 * 60 * 1000; // 1 hour\nconst MAX_MESSAGES = 5;               // Use only the last 5 messages\nconst MAX_MESSAGE_LENGTH = 100;        // Truncate messages to save tokens\n\ninterface CacheEntry {\n  suggestion: string;\n  timestamp: number;\n}\n\nconst suggestionCache = new Map<string, CacheEntry>();\n\nfunction generateCacheKey(messages: any[], orderStatus: string): string {\n  const relevantData = messages\n    .map(m => `${m.sender}:${m.text.substring(0, MAX_MESSAGE_LENGTH)}`)\n    .join('|');\n  const hash = createHash('sha256').update(relevantData + orderStatus).digest('hex');\n  return hash;\n}\n\nfunction getCachedSuggestion(key: string): string | null {\n  const entry = suggestionCache.get(key);\n  if (entry && Date.now() - entry.timestamp < CACHE_TTL_MS) {\n    return entry.suggestion;\n  }\n  suggestionCache.delete(key);\n  return null;\n}\n\nfunction setCachedSuggestion(key: string, suggestion: string): void {\n  if (suggestionCache.size > 100) {\n    const oldestKey = suggestionCache.keys().next().value;\n    suggestionCache.delete(oldestKey);\n  }\n  suggestionCache.set(key, { suggestion, timestamp: Date.now() });\n}\n\ninterface DeepSeekMessage {\n  role: 'system' | 'user' | 'assistant';\n  content: string;\n}\n\nfunction detectLanguage(text: string): string | null {\n  if (!text || text.trim().length < 10) return null; // Too short for reliable detection\n  const code = franc(text, { minLength: 3 });\n  if (code === 'und') return null;\n  const name = iso6391.getName(code);\n  return name || code;\n}\n\nexport async function callDeepSeek(\n  messages: DeepSeekMessage[],\n  options?: { maxTokens?: number; temperature?: number }\n): Promise<string> {\n  if (!process.env.DEEPSEEK_API_KEY) {\n    throw new Error('DEEPSEEK_API_KEY not configured');\n  }\n\n  try {\n    const response = await axios.post(\n      DEEPSEEK_API_URL,\n      {\n        model: 'deepseek-chat',\n        messages,\n        max_tokens: options?.maxTokens ?? 150,\n        temperature: options?.temperature ?? 0.7,\n      },\n      {\n        headers: {\n          'Authorization': `Bearer ${process.env.DEEPSEEK_API_KEY}`,\n          'Content-Type': 'application/json',\n        },\n        timeout: 8000,\n      }\n    );\n    return response.data.choices[0].message.content;\n  } catch (error) {\n    logger.error({ error }, 'DeepSeek API call failed');\n    throw error;\n  }\n}\n\nexport async function getReplySuggestion(\n  messageHistory: any[],\n  orderStatus: string\n): Promise<string> {\n  // 1. Reduce and truncate messages\n  const recentMessages = messageHistory.slice(-MAX_MESSAGES).map(m => ({\n    sender: m.sender,\n    text: m.text.length > MAX_MESSAGE_LENGTH ? m.text.substring(0, MAX_MESSAGE_LENGTH) + '…' : m.text,\n  }));\n\n  // 2. Generate cache key\n  const cacheKey = generateCacheKey(recentMessages, orderStatus);\n  const cached = getCachedSuggestion(cacheKey);\n  if (cached) {\n    logger.debug('Returning cached suggestion');\n    return cached;\n  }\n\n  // 3. Extract last customer message and detect language\n  const lastCustomerMsg = messageHistory\n    .filter(m => m.sender === 'customer')\n    .pop();\n  \n  let languageInstruction = 'Respond in a friendly, professional tone.';\n  if (lastCustomerMsg && lastCustomerMsg.text.trim().length >= 3) {\n    const detectedLang = detectLanguage(lastCustomerMsg.text);\n    if (detectedLang) {\n      languageInstruction = `IMPORTANT: The customer is writing in ${detectedLang}. You MUST respond in ${detectedLang}. Do not switch languages.`;\n    } else {\n      languageInstruction = `IMPORTANT: The customer's last message is: \"${lastCustomerMsg.text.substring(0, 200)}\". Respond in the same language as that message.`;\n    }\n  }\n\n  // 4. Build prompt\n  const formatted = recentMessages\n    .map(m => `${m.sender === 'customer' ? 'Customer' : 'Staff'}: ${m.text}`)\n    .join('\\n');\n\n  const systemPrompt = `You are a helpful assistant for restaurant staff. \nBased on the conversation history and the current order status (${orderStatus}), generate a short, friendly reply to the customer. \nThe reply should be at most 200 characters and match the tone of the conversation.\n${languageInstruction}`;\n\n  const userPrompt = `Message history:\\n${formatted}\\n\\nReply suggestion:`;\n\n  // 5. Call API\n  try {\n    const suggestion = await callDeepSeek([\n      { role: 'system', content: systemPrompt },\n      { role: 'user', content: userPrompt },\n    ], { maxTokens: 100, temperature: 0.5 });\n\n    const trimmed = suggestion.length > 200 ? suggestion.substring(0, 200) + '…' : suggestion;\n\n    setCachedSuggestion(cacheKey, trimmed);\n    return trimmed;\n  } catch (error) {\n    throw error;\n  }\n}\n"
        }
      ]
    },
    {
      "step": 3,
      "title": "Update documentation",
      "details": "Update README.md and ARCHITECTURE.md with the new language detection (if Option B is chosen).",
      "files": [
        {
          "path": "README.md",
          "action": "modify",
          "description": "Add a note about multilingual support and the libraries used.",
          "content": "### Multilingual AI Reply Assistant\n\nThe Reply Assistant now includes automatic language detection using `franc-min` and `iso-639-1`. When a customer sends a message, the system detects the language and instructs the AI to respond in the same language. This ensures seamless communication with international guests.\n\nIf the language cannot be reliably detected (very short messages), the AI is instructed to match the language of the last customer message via context.\n"
        },
        {
          "path": "ARCHITECTURE.md",
          "action": "modify",
          "description": "Add a description of the language detection to the DeepSeek service section.",
          "content": "#### Language Detection\n\nThe `getReplySuggestion` function uses `franc-min` to detect the language of the last customer message. The detected language (e.g., 'English', 'German') is passed to the AI via a system prompt instruction. If detection fails (e.g., message too short), the AI is prompted to infer the language from the message text itself.\n\nThis approach adds minimal overhead (only a few milliseconds) and no significant token cost.\n"
        }
      ]
    }
  ],
  "notes": [
    "Option A improves language detection through a stronger prompt and longer message retention but requires no additional dependencies.",
    "Option B is more precise because the language is explicitly detected and named. The libraries 'franc-min' and 'iso-639-1' are very lightweight (approx. 50 KB and 10 KB respectively) and ideal for this purpose.",
    "For very short messages (<10 characters), detection is skipped and context is relied upon.",
    "The cache structure remains unchanged because the cache key is still based on truncated messages – this is acceptable, as different languages produce different texts and thus different cache entries."
  ]
}
```