{
  "meta": {
    "prompt_id": "30.6-v1.0-Ultimate",
    "role": "Senior QA Automation Engineer",
    "purpose": "Create a single, deterministic, production-grade integration test suite covering ALL features from Prompts 27 (PDF Extraction), 28 (ProductCatalog UI), 29 (AI Chat Ordering), and 30.x (Edge Cases) – with zero flakiness, zero real API costs, and full backend contract validation.",
    "prerequisites": [
      "Prompts 27, 28, 29, and 30.1-30.4 are fully implemented and passing manual tests.",
      "Vitest, Supertest, and @vitest/coverage-v8 are installed: `npm install -D vitest @vitest/coverage-v8 supertest`",
      "Project uses Drizzle ORM with Neon PostgreSQL and TypeScript strict mode"
    ],
    "scope": "STRICTLY: Backend API integration tests (Supertest) + pure utility unit tests (Vitest). DO NOT write React DOM/UI tests, Playwright, or Cypress – avoid flaky timeouts and browser dependencies."
  },
  
  "critical_directive": "⚠️ SENIOR QA MODE: You are building the FINAL safety net for an Enterprise SaaS system. Write isolated, deterministic tests that: (1) NEVER make real DeepSeek or image-gen API calls (use vi.mock), (2) use SQL transaction rollback after EACH test for perfect isolation, (3) use exact expect() assertions with clear failure messages, (4) achieve ≥90% branch coverage on ALL modified files from Prompts 27-29+30. If you start writing UI tests, forget to mock external calls, or use vague assertions, STOP immediately.",
  
  "project_context": {
    "name": "Bistro Buzzer",
    "platform": "Replit (Node.js/Express, PostgreSQL/Neon, Drizzle ORM, TypeScript, React/Vite frontend)",
    "test_runner": "Vitest with @vitest/coverage-v8 + Supertest for HTTP",
    "features_under_test": {
      "prompt_27": [
        "GET /api/products with dynamic categories, variants, caching headers",
        "Product extraction pipeline (PDF → DeepSeek → DB) – mocked",
        "WebSocket MENU_UPDATED broadcast after extraction",
        "Zod validation + Drizzle schema with variants JSONB"
      ],
      "prompt_28": [
        "Frontend hooks for WebSocket listeners (tested via mock events)",
        "Cart injection logic (natural language formatting) – pure function test",
        "Variant-aware price calculation – pure function test"
      ],
      "prompt_29": [
        "POST /api/orders/:id/chat with variant-aware search_products tool",
        "Smart upselling logic (food-only → pairing suggestion)",
        "POST /api/orders/:id/confirm-order with server-side price validation",
        "Idempotency via client-generated UUID",
        "Transaction safety for order_items insertion",
        "WebSocket broadcast to admin dashboard"
      ],
      "prompt_30": [
        "Stock validation (isActive flag) in /confirm-order",
        "Thermal receipt formatting (32-char ASCII, emoji stripping)",
        "Chat soft-lock state management – pure function test"
      ]
    },
    "environment_strategy": {
      "test_db": "Use DATABASE_URL with ?schema=test or append _test suffix",
      "mock_api_keys": "DEEPSEEK_API_KEY=sk-test-mock-key, IMAGE_GEN_API_KEY=sk-test-mock",
      "log_level": "LOG_LEVEL=error (suppress debug during tests)",
      "websocket_mock": "Mock pub/sub system to avoid real WS connections in tests"
    }
  },
  
  "objective": "Implement a single, unified test suite that validates the ENTIRE ordering workflow end-to-end: PDF extraction → product catalog → chat ordering → confirmation → admin notification – with mocked external dependencies, transaction-based DB isolation, and ≥90% branch coverage on all modified code from Prompts 27-29+30.",
  
  "deliverables": [
    {
      "file": "vitest.config.ts",
      "task": "Configure Vitest for isolated, coverage-enforced backend testing",
      "code_requirements": [
        "import { defineConfig } from 'vitest/config';",
        "import { resolve } from 'path';",
        "",
        "export default defineConfig({",
        "  resolve: { alias: { '@': resolve(__dirname, './') } },",
        "  test: {",
        "    environment: 'node',",
        "    globals: true,",
        "    setupFiles: ['./server/__tests__/setup.ts'],",
        "    coverage: {",
        "      provider: 'v8',",
        "      reporter: ['text', 'json', 'html'],",
        "      exclude: ['node_modules/**', 'client/**', '**/*.d.ts'],",
        "      thresholds: {",
        "        statements: 90,",
        "        branches: 90,",
        "        functions: 90,",
        "        lines: 90",
        "      }",
        "    },",
        "    env: {",
        "      NODE_ENV: 'test',",
        "      LOG_LEVEL: 'error',",
        "      DEEPSEEK_API_KEY: 'sk-test-mock-key',",
        "      IMAGE_GEN_API_KEY: 'sk-test-mock'",
        "    },",
        "    // Prevent connection limit issues on Replit free tier",
        "    poolOptions: { threads: { singleThread: true } },",
        "    // Ensure clean exit",
        "    fileParallelism: false",
        "  }",
        "});"
      ]
    },
    {
      "file": "server/__tests__/setup.ts",
      "task": "Global test setup with per-test transaction isolation and mock utilities",
      "code_requirements": [
        "import { db } from '@/server/db';",
        "import { sql } from 'drizzle-orm';",
        "import { vi, beforeAll, afterAll, beforeEach, afterEach } from 'vitest';",
        "",
        "// Mock WebSocket pub/sub to avoid real connections",
        "vi.mock('@/server/websocket/pubsub', () => ({",
        "  publishToChannel: vi.fn().mockResolvedValue(undefined),",
        "  subscribeToChannel: vi.fn().mockReturnValue(() => {})",
        "}));",
        "",
        "// Mock image generation service",
        "vi.mock('@/server/lib/generate-product-image', () => ({",
        "  generatePlaceholderImage: vi.fn().mockResolvedValue(null)",
        "}));",
        "",
        "// Global transaction management for test isolation",
        "let transactionDepth = 0;",
        "",
        "export async function setupGlobal() {",
        "  // Ensure clean state before test suite",
        "  await db.execute(sql`ROLLBACK`); // In case of previous failure",
        "}",
        "",
        "export async function setupTest() {",
        "  // Start nested transaction for this test",
        "  await db.execute(sql`BEGIN`);",
        "  transactionDepth++;",
        "}",
        "",
        "export async function teardownTest() {",
        "  // Always rollback to clean state",
        "  await db.execute(sql`ROLLBACK`);",
        "  transactionDepth--;",
        "  vi.clearAllMocks();",
        "}",
        "",
        "export async function teardownGlobal() {",
        "  // Ensure all transactions are closed",
        "  while (transactionDepth > 0) {",
        "    await db.execute(sql`ROLLBACK`);",
        "    transactionDepth--;",
        "  }",
        "  // Close DB connection to prevent hanging",
        "  await (db as any).client?.end?.();",
        "}",
        "",
        "// Register hooks",
        "beforeAll(setupGlobal);",
        "afterAll(teardownGlobal);",
        "beforeEach(setupTest);",
        "afterEach(teardownTest);",
        "",
        "// Export test DB for direct queries in tests",
        "export { db as testDb };",
        "",
        "// Helper: create test order with unique ID",
        "export function createTestOrderId(prefix = 'test'): string {",
        "  return `${prefix}-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`;",
        "}"
      ]
    },
    {
      "file": "server/__tests__/products-api.integration.test.ts",
      "task": "Test GET /api/products endpoint from Prompt 27 (dynamic categories, variants, caching)",
      "code_requirements": [
        "import { describe, it, expect } from 'vitest';",
        "import request from 'supertest';",
        "import { app } from '@/server/app';",
        "import { testDb, createTestOrderId } from '@/server/__tests__/setup';",
        "import { products } from '@/shared/db-schema';",
        "import { eq } from 'drizzle-orm';",
        "",
        "describe('GET /api/products (Prompt 27)', () => {",
        "  it('returns products with dynamic categories (no hardcoded enums)', async () => {",
        "    // Insert test products with custom categories",
        "    await testDb.insert(products).values([",
        "      { name: 'Test Pizza', category: 'Italian Specials', price: 12.50, isActive: true },",
        "      { name: 'Test Burger', category: 'American Classics', price: 9.99, isActive: true }",
        "    ]);",
        "    ",
        "    const response = await request(app).get('/api/products');",
        "    expect(response.status).toBe(200);",
        "    expect(response.body.products).toBeInstanceOf(Array);",
        "    expect(response.body.products.some((p: any) => p.category === 'Italian Specials')).toBe(true);",
        "  });",
        "  ",
        "  it('returns variants JSONB array when present', async () => {",
        "    await testDb.insert(products).values({",
        "      name: 'Test Cola',",
        "      category: 'Drinks',",
        "      price: 3.50,",
        "      isActive: true,",
        "      variants: JSON.stringify([{ name: 'Small', price: 2.50 }, { name: 'Large', price: 4.50 }])",
        "    });",
        "    ",
        "    const response = await request(app).get('/api/products?category=Drinks');",
        "    expect(response.status).toBe(200);",
        "    const product = response.body.products.find((p: any) => p.name === 'Test Cola');",
        "    expect(product.variants).toEqual([",
        "      { name: 'Small', price: 2.50 },",
        "      { name: 'Large', price: 4.50 }",
        "    ]);",
        "  });",
        "  ",
        "  it('includes Cache-Control headers for CDN/browser caching', async () => {",
        "    const response = await request(app).get('/api/products');",
        "    expect(response.headers['cache-control']).toMatch(/public, s-maxage=300, stale-while-revalidate=60/);",
        "    expect(response.headers).toHaveProperty('etag');",
        "  });",
        "  ",
        "  it('filters by category (exact match) and search (ILIKE)', async () => {",
        "    await testDb.insert(products).values([",
        "      { name: 'Margherita Pizza', category: 'Pizza', price: 10, isActive: true },",
        "      { name: 'Pepperoni Pizza', category: 'Pizza', price: 12, isActive: true },",
        "      { name: 'Cola', category: 'Drinks', price: 3, isActive: true }",
        "    ]);",
        "    ",
        "    // Category filter",
        "    const catResponse = await request(app).get('/api/products?category=Pizza');",
        "    expect(catResponse.body.products.every((p: any) => p.category === 'Pizza')).toBe(true);",
        "    ",
        "    // Search filter (case-insensitive)",
        "    const searchResponse = await request(app).get('/api/products?search=margherita');",
        "    expect(searchResponse.body.products[0].name).toBe('Margherita Pizza');",
        "  });",
        "  ",
        "  it('excludes inactive products by default', async () => {",
        "    await testDb.insert(products).values([",
        "      { name: 'Active Item', category: 'Test', price: 5, isActive: true },",
        "      { name: 'Inactive Item', category: 'Test', price: 5, isActive: false }",
        "    ]);",
        "    ",
        "    const response = await request(app).get('/api/products?category=Test');",
        "    expect(response.body.products.some((p: any) => p.name === 'Inactive Item')).toBe(false);",
        "    expect(response.body.products.some((p: any) => p.name === 'Active Item')).toBe(true);",
        "  });",
        "});"
      ]
    },
    {
      "file": "server/__tests__/chat-ordering-flow.integration.test.ts",
      "task": "Test END-TO-END chat ordering flow: search → upsell → confirm → admin broadcast (Prompts 29 + 30)",
      "code_requirements": [
        "import { describe, it, expect, vi, beforeEach } from 'vitest';",
        "import request from 'supertest';",
        "import { app } from '@/server/app';",
        "import { testDb, createTestOrderId } from '@/server/__tests__/setup';",
        "import { products, orders, orderItems, idempotencyKeys } from '@/shared/db-schema';",
        "import { eq, sql } from 'drizzle-orm';",
        "import { publishToChannel } from '@/server/websocket/pubsub';",
        "",
        "// Mock DeepSeek client BEFORE importing chat handler",
        "vi.mock('@/server/lib/deepseek-client', () => ({",
        "  callDeepSeek: vi.fn()",
        "}));",
        "import { callDeepSeek } from '@/server/lib/deepseek-client';",
        "",
        "describe('End-to-End Chat Ordering Flow (Prompts 29 + 30)', () => {",
        "  let orderId: string;",
        "  let pizzaId: number;",
        "  let colaId: number;",
        "  ",
        "  beforeEach(async () => {",
        "    orderId = createTestOrderId('e2e');",
        "    await testDb.insert(orders).values({ id: orderId, status: 'waiting' });",
        "    ",
        "    // Create test products with variants",
        "    const [pizza] = await testDb.insert(products).values({",
        "      name: 'Pizza Margherita',",
        "      category: 'Pizza',",
        "      price: 10.00,",
        "      isActive: true,",
        "      variants: JSON.stringify([{ name: 'Small', price: 8.00 }, { name: 'Large', price: 12.00 }]),",
        "      tags: ['vegetarian']",
        "    }).returning({ id: products.id });",
        "    pizzaId = pizza.id;",
        "    ",
        "    const [cola] = await testDb.insert(products).values({",
        "      name: 'Cola',",
        "      category: 'Drinks',",
        "      price: 3.50,",
        "      isActive: true",
        "    }).returning({ id: products.id });",
        "    colaId = cola.id;",
        "    ",
        "    // Default DeepSeek mock: returns food-only order JSON",
        "    vi.mocked(callDeepSeek).mockResolvedValue({",
        "      content: '```json\\n{\"intent\":\"order\",\"items\":[{\"product_id\":' + pizzaId + ',\"name\":\"Pizza Margherita\",\"variant_name\":\"Large\",\"quantity\":2}]}\\n```',",
        "      tool_calls: null",
        "    });",
        "  });",
        "  ",
        "  it('completes full flow: chat → JSON preview → confirm → admin broadcast', async () => {",
        "    // STEP 1: User sends chat message",
        "    const chatResponse = await request(app)",
        "      .post(`/api/orders/${orderId}/chat`)",
        "      .send({ message: 'I want two large margherita pizzas' });",
        "    ",
        "    expect(chatResponse.status).toBe(200);",
        "    expect(chatResponse.body.order_preview).toMatchObject({",
        "      intent: 'order',",
        "      items: [{ product_id: pizzaId, variant_name: 'Large', quantity: 2 }]",
        "    });",
        "    ",
        "    // STEP 2: Smart upsell appended (food-only → drink suggestion)",
        "    const { reply } = chatResponse.body;",
        "    expect(reply).toMatch(/```json[\\s\\S]*```/); // JSON block present",
        "    const afterJson = reply.slice(reply.lastIndexOf('```') + 3);",
        "    expect(afterJson).toMatch(/💡.*(?:wine|beer|soda|pair)/i); // Specific pairing, not generic",
        "    ",
        "    // STEP 3: Client confirms order with idempotency key",
        "    const idempotencyKey = 'e2e-test-uuid-' + Date.now();",
        "    const confirmResponse = await request(app)",
        "      .post(`/api/orders/${orderId}/confirm-order`)",
        "      .send({",
        "        items: [{ product_id: pizzaId, variant_name: 'Large', quantity: 2 }],",
        "        idempotencyKey,",
        "        clientTimestamp: Date.now()",
        "      });",
        "    ",
        "    expect(confirmResponse.status).toBe(200);",
        "    expect(confirmResponse.body.success).toBe(true);",
        "    expect(confirmResponse.body.total).toBe(24.00); // 2 x Large price (12.00)",
        "    ",
        "    // STEP 4: Verify DB state – server price used, not client price",
        "    const items = await testDb.select()",
        "      .from(orderItems)",
        "      .where(eq(orderItems.orderId, orderId));",
        "    expect(items).toHaveLength(1);",
        "    expect(Number(items[0].priceAtTime)).toBe(12.00); // Variant price from DB",
        "    expect(items[0].variantName).toBe('Large');",
        "    expect(items[0].quantity).toBe(2);",
        "    ",
        "    // STEP 5: Verify idempotency key stored",
        "    const keyRecord = await testDb.query.idempotencyKeys.findFirst({",
        "      where: eq(idempotencyKeys.key, idempotencyKey)",
        "    });",
        "    expect(keyRecord).toBeDefined();",
        "    ",
        "    // STEP 6: Verify WebSocket broadcast to admin (mocked)",
        "    expect(vi.mocked(publishToChannel)).toHaveBeenCalledWith(",
        "      expect.stringContaining('admin:orders:'),",
        "      expect.objectContaining({ type: 'ORDER_ITEMS_ADDED' })",
        "    );",
        "  });",
        "  ",
        "  it('enforces stock validation: rejects inactive product during confirmation', async () => {",
        "    // Mark pizza as inactive AFTER chat but BEFORE confirmation (race condition test)",
        "    await testDb.update(products)",
        "      .set({ isActive: false })",
        "      .where(eq(products.id, pizzaId));",
        "    ",
        "    const confirmResponse = await request(app)",
        "      .post(`/api/orders/${orderId}/confirm-order`)",
        "      .send({",
        "        items: [{ product_id: pizzaId, quantity: 1 }],",
        "        idempotencyKey: 'stock-test-uuid'",
        "      });",
        "    ",
        "    expect(confirmResponse.status).toBe(400);",
        "    expect(confirmResponse.body.error).toMatchObject({",
        "      code: 'ITEM_UNAVAILABLE',",
        "      itemName: 'Pizza Margherita'",
        "    });",
        "    ",
        "    // Verify NO order_items inserted",
        "    const items = await testDb.select()",
        "      .from(orderItems)",
        "      .where(eq(orderItems.orderId, orderId));",
        "    expect(items).toHaveLength(0);",
        "  });",
        "  ",
        "  it('handles variant price resolution correctly (not base price)', async () => {",
        "    // Client tries to send base price (10.00) but variant is Large (12.00)",
        "    const confirmResponse = await request(app)",
        "      .post(`/api/orders/${orderId}/confirm-order`)",
        "      .send({",
        "        items: [{",
        "          product_id: pizzaId,",
        "          variant_name: 'Large',",
        "          quantity: 1,",
        "          _malicious_client_price: 0.01 // Attempted price manipulation",
        "        }],",
        "        idempotencyKey: 'variant-price-test'",
        "      });",
        "    ",
        "    expect(confirmResponse.status).toBe(200);",
        "    const items = await testDb.select()",
        "      .from(orderItems)",
        "      .where(eq(orderItems.orderId, orderId));",
        "    // Server MUST use DB variant price, NOT client-sent value",
        "    expect(Number(items[0].priceAtTime)).toBe(12.00); // Large price",
        "    expect(items[0].priceAtTime).not.toBe(0.01); // Client manipulation rejected",
        "  });",
        "  ",
        "  it('respects idempotency: same UUID returns duplicate:true without double-insert', async () => {",
        "    const idempotencyKey = 'idem-e2e-test';",
        "    const payload = {",
        "      items: [{ product_id: pizzaId, variant_name: 'Large', quantity: 1 }],",
        "      idempotencyKey",
        "    };",
        "    ",
        "    // First confirmation",
        "    const res1 = await request(app)",
        "      .post(`/api/orders/${orderId}/confirm-order`)",
        "      .send(payload);",
        "    expect(res1.body.duplicate).toBeUndefined();",
        "    ",
        "    // Second confirmation with SAME UUID",
        "    const res2 = await request(app)",
        "      .post(`/api/orders/${orderId}/confirm-order`)",
        "      .send(payload);",
        "    expect(res2.body.duplicate).toBe(true);",
        "    ",
        "    // Verify only ONE item inserted",
        "    const items = await testDb.select()",
        "      .from(orderItems)",
        "      .where(eq(orderItems.orderId, orderId));",
        "    expect(items).toHaveLength(1);",
        "  });",
        "  ",
        "  it('gracefully handles DeepSeek timeout with fallback message', async () => {",
        "    // Mock AbortError to simulate timeout",
        "    const abortError = new Error('The operation was aborted');",
        "    abortError.name = 'AbortError';",
        "    vi.mocked(callDeepSeek).mockRejectedValueOnce(abortError);",
        "    ",
        "    const response = await request(app)",
        "      .post(`/api/orders/${orderId}/chat`)",
        "      .send({ message: 'I want pizza' });",
        "    ",
        "    // Should return 200 with helpful fallback, not 500",
        "    expect(response.status).toBe(200);",
        "    expect(response.body.reply).toMatch(/taking a bit long|rephrase|try again/i);",
        "    expect(response.body.meta?.error).toBe('timeout');",
        "  });",
        "});"
      ]
    },
    {
      "file": "client/src/lib/__tests__/pure-utils.test.ts",
      "task": "Unit test pure functions from Prompts 28 + 30 (no DOM, no side effects)",
      "code_requirements": [
        "import { describe, it, expect } from 'vitest';",
        "import { formatKitchenTicket } from '../ticket-formatter'; // Prompt 30.3",
        "import { formatCartToChatMessage } from '../cart-formatter'; // NEW: Prompt 28 cart injection",
        "import { isConfirmationPending } from '../chat-state-utils'; // NEW: Prompt 30.4 soft-lock",
        "",
        "describe('Pure Utility Functions (Prompts 28 + 30)', () => {",
        "  describe('formatKitchenTicket (Prompt 30.3)', () => {",
        "    const baseOrder = { id: 'ABC-123', createdAt: '2024-01-15T18:30:00Z' };",
        "    ",
        "    it('ensures EVERY line is exactly 32 characters', () => {",
        "      const result = formatKitchenTicket(baseOrder, [",
        "        { name: 'Pizza Margherita', quantity: 2, price: 12.50, variantName: 'Large' }",
        "      ]);",
        "      result.split('\\n').forEach((line, idx) => {",
        "        expect(line.length).toBe(32), `Line ${idx + 1}: \"${line}\" has ${line.length} chars`);",
        "      });",
        "    });",
        "    ",
        "    it('strips emojis and non-ASCII for thermal printer compatibility', () => {",
        "      const result = formatKitchenTicket(baseOrder, [",
        "        { name: 'Café 🍕 Pizza', quantity: 1, price: 10.00 }",
        "      ]);",
        "      expect(result).not.toContain('🍕');",
        "      expect(result).toContain('Caf'); // Accented char may be normalized",
        "    });",
        "    ",
        "    it('truncates long names with ellipsis and right-aligns price', () => {",
        "      const result = formatKitchenTicket(baseOrder, [",
        "        { name: 'Very Long Product Name Exceeding Thirty Two Character Limit', quantity: 1, price: 9.99 }",
        "      ]);",
        "      expect(result).toContain('...');",
        "      expect(result).toMatch(/\\s{3}9\\.99$/m); // Price in last 7 chars",
        "    });",
        "  });",
        "  ",
        "  describe('formatCartToChatMessage (Prompt 28)', () => {",
        "    it('formats cart items as natural language for AI chat injection', () => {",
        "      const cart = [",
        "        { name: 'Pizza Margherita', variantName: 'Large', quantity: 2, modifications: 'no onions', priceAtSelection: 12.00 },",
        "        { name: 'Cola', variantName: null, quantity: 1, modifications: null, priceAtSelection: 3.50 }",
        "      ];",
        "      const message = formatCartToChatMessage(cart);",
        "      expect(message).toContain(\"I'd like to order:\");",
        "      expect(message).toContain('2x Pizza Margherita (Large) – no onions');",
        "      expect(message).toContain('1x Cola');",
        "      expect(message).toContain('Total: €27.50');",
        "    });",
        "    ",
        "    it('handles empty cart gracefully', () => {",
        "      const message = formatCartToChatMessage([]);",
        "      expect(message).toBe(''); // Or appropriate empty state",
        "    });",
        "  });",
        "  ",
        "  describe('isConfirmationPending (Prompt 30.4)', () => {",
        "    it('returns true when AI message has unconfirmed order_preview', () => {",
        "      const message = {",
        "        sender: 'ai',",
        "        order_preview: { intent: 'order', items: [] },",
        "        confirmed: false,",
        "        cancelled: false",
        "      };",
        "      expect(isConfirmationPending(message)).toBe(true);",
        "    });",
        "    ",
        "    it('returns false when order was confirmed or cancelled', () => {",
        "      const confirmedMsg = { sender: 'ai', order_preview: {}, confirmed: true };",
        "      const cancelledMsg = { sender: 'ai', order_preview: {}, cancelled: true };",
        "      expect(isConfirmationPending(confirmedMsg)).toBe(false);",
        "      expect(isConfirmationPending(cancelledMsg)).toBe(false);",
        "    });",
        "    ",
        "    it('returns false for non-order messages', () => {",
        "      const regularMsg = { sender: 'ai', content: 'Hello!' };",
        "      expect(isConfirmationPending(regularMsg)).toBe(false);",
        "    });",
        "  });",
        "});"
      ]
    },
    {
      "file": "server/__tests__/extraction-pipeline.mock.test.ts",
      "task": "Test PDF extraction pipeline logic with mocked DeepSeek (Prompt 27) – NO real PDFs or API calls",
      "code_requirements": [
        "import { describe, it, expect, vi, beforeEach } from 'vitest';",
        "import { ProductExtractor } from '@/server/lib/extract-products'; // Prompt 27",
        "import { testDb } from '@/server/__tests__/setup';",
        "import { products } from '@/shared/db-schema';",
        "import { eq } from 'drizzle-orm';",
        "",
        "// Mock pdf-parse and DeepSeek fetch",
        "vi.mock('pdf-parse', () => ({ default: vi.fn() }));",
        "import pdf from 'pdf-parse';",
        "",
        "vi.mock('@/server/lib/deepseek-client', () => ({",
        "  callDeepSeek: vi.fn()",
        "}));",
        "import { callDeepSeek } from '@/server/lib/deepseek-client';",
        "",
        "describe('ProductExtractor Pipeline (Prompt 27 – Mocked)', () => {",
        "  let extractor: ProductExtractor;",
        "  ",
        "  beforeEach(() => {",
        "    extractor = new ProductExtractor(testDb);",
        "    vi.clearAllMocks();",
        "  });",
        "  ",
        "  it('extracts products with dynamic categories (no hardcoded enums)', async () => {",
        "    // Mock PDF text extraction",
        "    vi.mocked(pdf).mockResolvedValue({ text: 'Special Pizza – €10.00 – Category: Italian Specials' } as any);",
        "    ",
        "    // Mock DeepSeek response with custom category",
        "    vi.mocked(callDeepSeek).mockResolvedValue({",
        "      choices: [{ message: { content: JSON.stringify([{ name: 'Special Pizza', price: 10.00, category: 'Italian Specials' }]) } }]",
        "    } as any);",
        "    ",
        "    // Run extraction (dry-run to avoid DB writes in this unit test)",
        "    const summary = await extractor.extractAll(true);",
        "    ",
        "    expect(summary.processed).toBe(1);",
        "    expect(summary.skipped).toBe(0);",
        "    // Verify category was preserved as-is (not mapped to enum)",
        "    expect(vi.mocked(callDeepSeek)).toHaveBeenCalledWith(",
        "      expect.objectContaining({",
        "        messages: expect.arrayContaining([",
        "          expect.objectContaining({",
        "            content: expect.stringContaining('Italian Specials')",
        "          })",
        "        ])",
        "      }),",
        "      expect.any(AbortSignal)",
        "    );",
        "  });",
        "  ",
        "  it('handles variants array in DeepSeek response', async () => {",
        "    vi.mocked(pdf).mockResolvedValue({ text: 'Cola – Small €2.50, Large €4.50' } as any);",
        "    ",
        "    vi.mocked(callDeepSeek).mockResolvedValue({",
        "      choices: [{ message: { content: JSON.stringify([{",
        "        name: 'Cola',",
        "        category: 'Drinks',",
        "        variants: [{ name: 'Small', price: 2.50 }, { name: 'Large', price: 4.50 }]",
        "      }]) } }]",
        "    } as any);",
        "    ",
        "    const summary = await extractor.extractAll(true);",
        "    expect(summary.processed).toBe(1);",
        "    // Verify variants were passed through",
        "    const callArgs = vi.mocked(callDeepSeek).mock.calls[0][0];",
        "    expect(callArgs.body).toContain('variants');",
        "  });",
        "  ",
        "  it('respects AbortController timeout (25s)', async () => {",
        "    vi.mocked(pdf).mockResolvedValue({ text: 'Test' } as any);",
        "    ",
        "    // Mock DeepSeek to delay beyond timeout",
        "    vi.mocked(callDeepSeek).mockImplementation(async (_, signal) => {",
        "      return new Promise((_, reject) => {",
        "        setTimeout(() => reject(new Error('AbortError')), 30000); // 30s > 25s timeout",
        "        signal?.addEventListener('abort', () => reject(new Error('AbortError')));",
        "      });",
        "    });",
        "    ",
        "    // Should handle timeout gracefully, not crash",
        "    const summary = await extractor.extractAll(true);",
        "    expect(summary.errors).toContainEqual(expect.stringContaining('timeout'));",
        "  });",
        "  ",
        "  it('deduplicates products by name+category using fuzzy mapping context', async () => {",
        "    // Insert existing product for fuzzy mapping test",
        "    await testDb.insert(products).values({",
        "      name: 'Spaghetti Bolognese',",
        "      category: 'Pasta',",
        "      price: 11.00,",
        "      isActive: true",
        "    });",
        "    ",
        "    vi.mocked(pdf).mockResolvedValue({ text: 'Spag. Bolo – €11.00' } as any);",
        "    ",
        "    // Mock DeepSeek to return existing_product_id for fuzzy match",
        "    vi.mocked(callDeepSeek).mockResolvedValue({",
        "      choices: [{ message: { content: JSON.stringify([{",
        "        name: 'Spaghetti Bolognese', // Mapped to existing name",
        "        existing_product_id: 1, // Hypothetical ID",
        "        price: 11.00,",
        "        category: 'Pasta'",
        "      }]) } }]",
        "    } as any);",
        "    ",
        "    const summary = await extractor.extractAll(true);",
        "    // Should update existing, not insert duplicate",
        "    expect(summary.inserted).toBe(0);",
        "    expect(summary.updated).toBe(1);",
        "  });",
        "});"
      ]
    },
    {
      "file": "package.json",
      "task": "Add comprehensive test scripts with coverage and CI support",
      "code_requirements": [
        "// Add to scripts object:",
        "\"test\": \"vitest run\",",
        "\"test:watch\": \"vitest\",",
        "\"test:coverage\": \"vitest run --coverage\",",
        "\"test:ci\": \"vitest run --coverage --reporter=junit --outputFile.junit=reports/test-results.xml\",",
        "\"test:e2e\": \"vitest run server/__tests__/chat-ordering-flow.integration.test.ts\""
      ]
    },
    {
      "file": ".env.test",
      "task": "Environment variables for complete test isolation",
      "content": [
        "# Test environment – NEVER use production values",
        "NODE_ENV=test",
        "DATABASE_URL=postgresql://test:test@localhost:5432/bistro_buzzer_test?schema=public",
        "LOG_LEVEL=error",
        "DEEPSEEK_API_KEY=sk-test-mock-key-do-not-use",
        "IMAGE_GEN_API_KEY=sk-test-mock-image",
        "SESSION_SECRET=test-secret-min-64-chars-abcdefghijklmnopqrstuvwxyz0123456789",
        "ALLOWED_ORIGIN=http://localhost:3000",
        "VAPID_PUBLIC_KEY=test-vapid-public",
        "VAPID_PRIVATE_KEY=test-vapid-private"
      ]
    }
  ],
  
  "testing_checkpoints": [
    {
      "step": 1,
      "command": "npm run test",
      "expected": "All 4 test files run successfully. Zero hanging connections. Exit code 0. Output shows: 'Test Files 4 passed, Tests 25 passed'."
    },
    {
      "step": 2,
      "command": "npm run test:coverage",
      "expected": "Coverage report shows ≥90% branch coverage for: extract-products.ts, orders.ts, chat.ts, ticket-formatter.ts, cart-formatter.ts. HTML report in coverage/."
    },
    {
      "step": 3,
      "command": "Verify no real API calls",
      "expected": "Console shows ZERO fetch calls to api.deepseek.com or image-gen APIs. vi.mocked intercepts all external calls."
    },
    {
      "step": 4,
      "command": "Check DB state after tests",
      "expected": "Test data (orders/products with 'test-' or 'e2e-' prefix) is rolled back – production DB untouched. Run: psql $DATABASE_URL -c \"SELECT COUNT(*) FROM orders WHERE id LIKE 'test-%';\" → returns 0."
    },
    {
      "step": 5,
      "command": "Run end-to-end flow test specifically",
      "expected": "npm run test:e2e passes all 5 tests in chat-ordering-flow.integration.test.ts, validating full chat → confirm → admin broadcast pipeline."
    },
    {
      "step": 6,
      "command": "Simulate CI: npm run test:ci",
      "expected": "JUnit XML report generated in reports/test-results.xml – compatible with Replit CI, GitHub Actions, or GitLab CI."
    },
    {
      "step": 7,
      "command": "TypeScript check",
      "expected": "npx tsc --noEmit returns zero errors across all test files and source code."
    }
  ],
  
  "replit_specific_notes": [
    "Replit auto-runs `npm test` on deploy – ensure tests exit cleanly (no hanging promises, DB connections closed via teardownGlobal)",
    "Use singleThread: true in vitest.config.ts to avoid Neon connection limit issues on free tier",
    "Transaction-based isolation (BEGIN/ROLLBACK per test) is safer than TRUNCATE for concurrent test runs",
    "Mock API keys in .env.test prevent accidental token usage during testing or CI",
    "Coverage thresholds (90%) ensure new edge-case logic from Prompts 27-29+30 is thoroughly tested before merge",
    "JUnit reporter enables integration with Replit's built-in test visualization or external CI",
    "If tests hang: add console logs in teardownGlobal to debug unclosed connections",
    "For large test suites: consider splitting into 'unit' and 'integration' folders with separate configs"
  ],
  
  "success_criteria": "Prompt 30.6-Ultimate is COMPLETE when:\n1. Four test files created: products-api.integration.test.ts, chat-ordering-flow.integration.test.ts, pure-utils.test.ts, extraction-pipeline.mock.test.ts\n2. vitest.config.ts configured with coverage thresholds ≥90%, singleThread pool, and proper env isolation\n3. Global setup.ts implements per-test transaction isolation with clean teardown\n4. ALL DeepSeek/image-gen calls mocked via vi.mock – zero real API requests during test run\n5. Running `npm run test` passes 100% of tests (≥25 tests) with exit code 0\n6. Running `npm run test:coverage` shows ≥90% branch coverage for ALL modified files from Prompts 27-29+30\n7. JUnit report generated for CI integration\n8. Zero TypeScript errors (npx tsc --noEmit)\n9. All pre-existing tests still pass (no regression)\n10. End-to-end flow test validates full ordering pipeline: chat → JSON preview → confirm → admin broadcast",
  
  "failure_handling": "If tests fail or hang:\n1. Check for unclosed DB connections – ensure teardownGlobal calls db.client?.end?.()\n2. Verify vi.mock is declared BEFORE importing modules that use mocked functions\n3. If coverage is <90%, add tests for edge cases: empty arrays, null values, error paths, variant resolution\n4. If flaky: add singleThread: true and fileParallelism: false to vitest.config.ts\n5. If DeepSeek mock fails: ensure callDeepSeek import path matches actual file structure\n6. DO NOT lower coverage thresholds, skip mocking, or remove transaction isolation – these are non-negotiable quality gates",
  
  "final_self_verification": "Before marking complete, run this ULTIMATE checklist:\n□ Are ALL DeepSeek/image-gen calls mocked via vi.mock in every test file?\n□ Does setup.ts start a transaction per test and teardown.ts rollback for perfect isolation?\n□ Does products-api.test.ts verify dynamic categories (no hardcoded enums) and variants JSONB?\n□ Does chat-ordering-flow.test.ts validate END-TO-END: chat → JSON → confirm → DB → WebSocket?\n□ Does confirm-order test enforce server-side price validation (never trust client price)?\n□ Does idempotency test send same UUID twice and verify duplicate:true + no double-insert?\n□ Does pure-utils.test.ts verify formatKitchenTicket enforces 32-char width AND strips emojis?\n□ Does extraction-pipeline.test.ts mock pdf-parse AND DeepSeek for safe unit testing?\n□ Does npm run test:coverage show ≥90% branch coverage for extract-products.ts, orders.ts, chat.ts?\n□ Do all tests exit cleanly with no hanging connections (teardownGlobal works)?\n□ Does npx tsc --noEmit return zero errors across source AND test files?\n□ Do all pre-existing tests still pass (no regression from new test suite)?\n\nIf ANY answer is NO: fix the specific issue before marking complete. This is your Enterprise safety net – make it bulletproof."
}