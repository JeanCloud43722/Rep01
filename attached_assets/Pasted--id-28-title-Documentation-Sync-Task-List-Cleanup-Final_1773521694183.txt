{
  "id": 28,
  "title": "Documentation Sync, Task List Cleanup & Final Production Verification",
  "priority": "P3-Low",
  "depends_on": [1, 2, 3, 10, 21, 23, 24, 25, 27],
  "context": "All 27 optimization prompts have been executed. The codebase is production-ready but documentation may be out of sync with implementation. Task lists may show items as TODO that are actually complete. Final verification ensures nothing was missed.",
  "task": "Synchronize all documentation with actual implementation, clean up task lists, and run final production verification checklist.",
  "detailed_instructions": [
    "README.md UPDATES:",
    "  - Verify all Environment Variables table entries match actual server/env-validation.ts implementation",
    "  - Update API Reference table to include new endpoints: POST /api/orders/:id/complete, POST /api/orders/:id/service/:requestId/acknowledge, POST /api/orders/cleanup, GET /api/health",
    "  - Add State Machine diagram showing valid transitions (waiting→subscribed→scheduled→notified→completed)",
    "  - Add Security section documenting: Helmet headers, input sanitization, rate limiting thresholds, CORS policy",
    "  - Add Testing section with vitest commands and test file locations",
    "  - Verify all deployment checklist steps are accurate and tested",
    "ARCHITECTURE.md SYNC:",
    "  - Update §2 Storage Layer: Document PostgreSQL/Drizzle implementation, JSONB strategy, indexes",
    "  - Update §3 Server Entry Point: Document env-validation, Winston logging, Helmet, CORS, rate limiting",
    "  - Update §4 REST API: Add new endpoints (complete, acknowledge, cleanup, health)",
    "  - Update §6 Push Pipeline: Document tracked retry logic, HTTP 404/410 handling, subscription cleanup",
    "  - Update §15 Order Lifecycle: Add state machine validation details, invalid transition error handling",
    "  - Update §16 Known Optimizations: Mark completed items with ✅ and date, remove or mark as done:",
    "    - ✅ PostgreSQL migration (completed)",
    "    - ✅ Admin authentication (completed)",
    "    - ✅ Push retry tracking (completed)",
    "    - ✅ Rate limiting (completed)",
    "    - ✅ Session cleanup (completed)",
    "    - ✅ Message pagination (completed)",
    "    - ✅ Cache auto-versioning (completed)",
    "TASK LIST CLEANUP:",
    "  - Review all TODO/FIXME comments in codebase",
    "  - Remove or resolve items that are now complete:",
    "    - AudioContext resume on interaction — already implemented, remove TODO",
    "    - Push permission re-prompt on interaction — already implemented, remove TODO",
    "    - Notification logging — already implemented, remove TODO",
    "  - Create TRACKING.md with remaining technical debt (if any):",
    "    - Future: GraphQL interface (if external API consumers emerge)",
    "    - Future: VAPID key rotation mechanism (if security audit requires)",
    "    - Future: Multi-language push notifications (if international expansion)",
    "FINAL VERIFICATION CHECKLIST:",
    "  - Run npm test — all 40+ tests must pass",
    "  - Run npm run build — no TypeScript errors, no build warnings",
    "  - Run npm run lint — no ESLint errors",
    "  - Start server, visit /api/health — verify database connected, version present",
    "  - Create order via admin, verify it persists after server restart",
    "  - Test invalid state transition (waiting→completed) — verify 400 error with clear message",
    "  - Test service request acknowledgment flow end-to-end",
    "  - Test XSS protection: send message with <script> tag — verify sanitized",
    "  - Test rate limiting: spam endpoint 10x — verify 429 after threshold",
    "  - Test on mobile device: PWA install, push notification, audio cues",
    "  - Check Replit Console: verify structured JSON logs appear (not console.log)",
    "  - Monitor Database panel: verify session table stays small (pruneSessionInterval working)",
    "DEPLOYMENT TAGGING:",
    "  - Update package.json version to 2.1.0 (or next semantic version)",
    "  - Create git tag: git tag -a v2.1.0 -m 'Production release with all optimizations'",
    "  - Add CHANGELOG.md entry summarizing all 28 prompts executed",
    "  - Document any manual steps required for deployment (Secrets setup, database binding)"
  ],
  "replit_note": "This is the final polish step before going live. Ensure documentation matches reality — future maintainers depend on accurate docs. The verification checklist catches any prompts that may have partially failed.",
  "testing_checkpoint": "README.md accurately reflects all implemented features. All TODOs resolved or documented in TRACKING.md. Final verification checklist passes 100%. Git tag created. Ready for production deployment."
}