{
  "meta": {
    "prompt_id": "31.4-v1.0-View-Toggles",
    "role": "Senior Frontend Engineer",
    "purpose": "Add flexible view modes (Grid/Kanban) to admin dashboard with localStorage persistence.",
    "prerequisites": [
      "Prompt 31.1 is complete (urgency sorting + visual cues)",
      "OrderCard component renders consistently in both layouts",
      "Tailwind CSS is configured"
    ],
    "scope": "STRICTLY: Add view toggle UI and Kanban layout to client/src/pages/admin.tsx. NO backend changes."
  },
  
  "critical_directive": "⚠️ SENIOR ENGINEER MODE: This is an ENHANCEMENT prompt. Grid view must remain the default and fully functional. Kanban view is additive – if implementation is complex, ship Grid view first and iterate. Do NOT break existing sorting or visual cues.",
  
  "deliverables": [
    {
      "file": "client/src/pages/admin.tsx",
      "task": "Add view toggle + implement Kanban layout with persistence",
      "code_requirements": [
        "import { ToggleGroup, ToggleGroupItem } from '@/components/ui/toggle-group';",
        "import { LayoutGrid, Columns } from 'lucide-react';",
        "",
        "// 1. State for view mode with localStorage persistence",
        "type ViewMode = 'grid' | 'kanban';",
        "const [viewMode, setViewMode] = useState<ViewMode>(() => {",
        "  // Load from localStorage on mount",
        "  if (typeof window !== 'undefined') {",
        "    const saved = localStorage.getItem('admin-view-mode') as ViewMode;",
        "    return saved || 'grid';",
        "  }",
        "  return 'grid';",
        "});",
        "",
        "// Save to localStorage when changed",
        "useEffect(() => {",
        "  localStorage.setItem('admin-view-mode', viewMode);",
        "}, [viewMode]);",
        "",
        "// 2. Toggle UI near search bar",
        "// Add to header section (after search, before actions):",
        "<ToggleGroup",
        "  type=\"single\"",
        "  value={viewMode}",
        "  onValueChange={(value) => value && setViewMode(value as ViewMode)}",
        "  className=\"bg-gray-100 rounded-lg p-1\"",
        ">",
        "  <ToggleGroupItem value=\"grid\" aria-label=\"Grid view\" className=\"data-[state=on]:bg-white\">",
        "    <LayoutGrid className=\"w-4 h-4 mr-1\" /> Grid",
        "  </ToggleGroupItem>",
        "  <ToggleGroupItem value=\"kanban\" aria-label=\"Kanban view\" className=\"data-[state=on]:bg-white\">",
        "    <Columns className=\"w-4 h-4 mr-1\" /> Kanban",
        "  </ToggleGroupItem>",
        "</ToggleGroup>",
        "",
        "// 3. Grid View (existing layout – keep unchanged)",
        "// Render when viewMode === 'grid':",
        "{viewMode === 'grid' && (",
        "  <div className=\"grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4\">",
        "    {sortedOrders.map(order => (",
        "      <OrderCard key={order.id} order={order} />",
        "    ))}",
        "  </div>",
        ")}",
        "",
        "// 4. Kanban View – three columns by status",
        "{viewMode === 'kanban' && (",
        "  <div className=\"flex gap-4 overflow-x-auto pb-4\">",
        "    ",
        "    {/* Column 1: Waiting/Scheduled */}",
        "    <div className=\"flex-shrink-0 w-80 bg-gray-50 rounded-xl p-3\">",
        "      <h4 className=\"font-semibold text-sm text-gray-700 mb-3 flex items-center gap-2\">",
        "        <span className=\"w-2 h-2 bg-yellow-400 rounded-full\" />",
        "        Waiting / Scheduled",
        "        <span className=\"ml-auto text-xs text-gray-400\">",
        "          {sortedOrders.filter(o => ['waiting', 'scheduled'].includes(o.status)).length}",
        "        </span>",
        "      </h4>",
        "      <div className=\"space-y-3\">",
        "        {sortedOrders",
        "          .filter(order => ['waiting', 'scheduled'].includes(order.status))",
        "          .map(order => (",
        "            <OrderCard key={order.id} order={order} variant=\"compact\" />",
        "          ))}",
        "      </div>",
        "    </div>",
        "    ",
        "    {/* Column 2: Preparing/Subscribed */}",
        "    <div className=\"flex-shrink-0 w-80 bg-gray-50 rounded-xl p-3\">",
        "      <h4 className=\"font-semibold text-sm text-gray-700 mb-3 flex items-center gap-2\">",
        "        <span className=\"w-2 h-2 bg-blue-400 rounded-full\" />",
        "        Preparing",
        "        <span className=\"ml-auto text-xs text-gray-400\">",
        "          {sortedOrders.filter(o => ['preparing', 'subscribed'].includes(o.status)).length}",
        "        </span>",
        "      </h4>",
        "      <div className=\"space-y-3\">",
        "        {sortedOrders",
        "          .filter(order => ['preparing', 'subscribed'].includes(order.status))",
        "          .map(order => (",
        "            <OrderCard key={order.id} order={order} variant=\"compact\" />",
        "          ))}",
        "      </div>",
        "    </div>",
        "    ",
        "    {/* Column 3: Ready/Notified/Completed */}",
        "    <div className=\"flex-shrink-0 w-80 bg-gray-50 rounded-xl p-3\">",
        "      <h4 className=\"font-semibold text-sm text-gray-700 mb-3 flex items-center gap-2\">",
        "        <span className=\"w-2 h-2 bg-green-400 rounded-full\" />",
        "        Ready / Done",
        "        <span className=\"ml-auto text-xs text-gray-400\">",
        "          {sortedOrders.filter(o => ['ready', 'notified', 'completed'].includes(o.status)).length}",
        "        </span>",
        "      </h4>",
        "      <div className=\"space-y-3\">",
        "        {sortedOrders",
        "          .filter(order => ['ready', 'notified', 'completed'].includes(order.status))",
        "          .map(order => (",
        "            <OrderCard key={order.id} order={order} variant=\"compact\" />",
        "          ))}",
        "      </div>",
        "    </div>",
        "  </div>",
        ")}",
        "",
        "// 5. OrderCard variant prop for compact Kanban display",
        "// In OrderCard component, add optional variant prop:",
        "interface OrderCardProps {",
        "  order: Order;",
        "  variant?: 'default' | 'compact'; // default = full, compact = minimal for Kanban",
        "}",
        "// In render: if variant === 'compact', hide less critical fields (description, timestamps)",
        "",
        "// 6. Responsive handling: on mobile, force Grid view or horizontal scroll for Kanban",
        "// Add media query or window resize listener if needed",
        "useEffect(() => {",
        "  const handleResize = () => {",
        "    if (window.innerWidth < 768 && viewMode === 'kanban') {",
        "      // Optional: auto-switch to grid on mobile, or keep kanban with horizontal scroll",
        "      // For now, keep kanban but ensure container is scrollable (already handled by overflow-x-auto)",
        "    }",
        "  };",
        "  window.addEventListener('resize', handleResize);",
        "  return () => window.removeEventListener('resize', handleResize);",
        "}, [viewMode]);"
      ]
    }
  ],
  
  "testing_checkpoints": [
    {
      "step": 1,
      "command": "Open admin dashboard, verify default view is Grid",
      "expected": "Orders display in masonry/grid layout (1-3 columns based on screen width)."
    },
    {
      "step": 2,
      "command": "Click 'Kanban' toggle",
      "expected": "Layout switches to 3 horizontal columns: Waiting/Scheduled | Preparing | Ready/Done. Orders grouped by status."
    },
    {
      "step": 3,
      "command": "Refresh page (or reopen in new tab)",
      "expected": "View mode persists (Kanban stays selected) via localStorage."
    },
    {
      "step": 4,
      "command": "In Kanban view, verify urgency sorting within columns",
      "expected": "Within each column, service requests appear first, then unread messages, then by age."
    },
    {
      "step": 5,
      "command": "Run npm test",
      "expected": "All pre-existing tests pass; Grid view unchanged, Kanban is additive."
    }
  ],
  
  "replit_specific_notes": [
    "If shadcn/ui ToggleGroup is not installed: use simple button group with Tailwind state classes (bg-white for active).",
    "Kanban columns: use flex-shrink-0 + overflow-x-auto container for horizontal scroll on smaller screens.",
    "OrderCard compact variant: hide non-essential fields to fit more cards in Kanban columns.",
    "localStorage key: use 'admin-view-mode' to avoid conflicts with other app settings."
  ],
  
  "success_criteria": "Prompt 31.4 is COMPLETE when:\n1. ToggleGroup UI shows Grid/Kanban options near search bar\n2. Grid view renders existing layout (unchanged from Prompt 31.1)\n3. Kanban view renders 3 status-based columns with horizontal scroll\n4. View mode persists across page reloads via localStorage\n5. Urgency sorting works within each Kanban column\n6. OrderCard supports compact variant for Kanban display\n7. All 5 testing checkpoints pass\n8. Zero TypeScript errors (npx tsc --noEmit)\n9. All pre-existing tests still pass",
  
  "failure_handling": "If Kanban layout is too complex:\n1. Ship Grid view first (already working from Prompt 31.1)\n2. Add Kanban as iterative improvement in next prompt\n3. Do NOT break Grid view while implementing Kanban\n4. If ToggleGroup fails: fallback to simple button group with Tailwind\n5. Wait for explicit confirmation before marking complete",
  
  "final_self_verification": "Before marking complete:\n□ Is Grid view the default and fully functional?\n□ Does ToggleGroup switch between Grid and Kanban views?\n□ Does Kanban show 3 columns grouped by status?\n□ Does view mode persist after page reload (localStorage)?\n□ Does urgency sorting work within each Kanban column?\n□ Does OrderCard support compact variant for Kanban?\n□ Is the layout responsive (horizontal scroll on small screens)?\n□ Does npx tsc --noEmit return 0 errors?\n□ Do all pre-existing tests still pass?\n\nIf ANY answer is NO: fix before proceeding."
}