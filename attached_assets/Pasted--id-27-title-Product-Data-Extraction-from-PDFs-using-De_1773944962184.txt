{
  "id": 27,
  "title": "Product Data Extraction from PDFs using DeepSeek – Production Hardened (v2)",
  "priority": "P1-High",
  "context": "The restaurant has menu and ice cream PDFs that need to be converted into structured product data for the catalog and ordering system. This pipeline must be robust, idempotent, and Replit-optimized.",
  "task": "Create a production-ready pipeline to extract product information from PDF files using DeepSeek API, store it in a PostgreSQL `products` table with conflict handling, and provide a cached, filterable API endpoint.",
  "detailed_instructions": [
    "## 1. Project Structure & Dependencies",
    "- Create folders `knowledge-base/menu/` and `knowledge-base/icecream/` if not present.",
    "- Install dependencies: `npm install pdf-parse glob zod openai`",
    "- Add to package.json scripts:",
    "  ```json",
    "  \"scripts\": {",
    "    \"extract-products\": \"tsx scripts/extract-products.ts\",",
    "    \"extract-products:dry\": \"tsx scripts/extract-products.ts --dry-run\"",
    "  }",
    "  ```",
    
    "## 2. Database Schema (shared/db-schema.ts)",
    "- Add the products table with proper indexes:",
    "```typescript",
    "export const products = pgTable('products', {",
    "  id: serial('id').primaryKey(),",
    "  name: text('name').notNull(),",
    "  description: text('description'),",
    "  price: numeric('price', { precision: 10, scale: 2 }).notNull(),",
    "  category: text('category').notNull(),",
    "  allergens: text('allergens').array().default([]),",
    "  tags: text('tags').array().default([]),",
    "  imageUrl: text('image_url'),",
    "  createdAt: timestamp('created_at').defaultNow(),",
    "  updatedAt: timestamp('updated_at').defaultNow()",
    "});",
    "```",
    "- Add indexes:",
    "```typescript",
    "export const productsNameIdx = index('products_name_idx').on(products.name);",
    "export const productsCategoryIdx = index('products_category_idx').on(products.category);",
    "export const productsTagsIdx = index('products_tags_idx').using('gin', products.tags);",
    "```",
    
    "## 3. Zod Schema for Validation (shared/schema.ts)",
    "```typescript",
    "export const productSchema = z.object({",
    "  name: z.string().min(1).max(100),",
    "  description: z.string().max(500).optional().or(z.null()),",
    "  price: z.number().positive().max(999.99),",
    "  category: z.enum(['Pizza', 'Pasta', 'Ice Cream', 'Drinks', 'Appetizers', 'Desserts', 'Other']),",
    "  allergens: z.array(z.string()).default([]),",
    "  tags: z.array(z.enum(['vegetarian', 'vegan', 'gluten-free', 'spicy', 'popular', 'seasonal'])).default([]),",
    "  image_url: z.string().url().optional().or(z.null())",
    "});",
    "export type ProductInput = z.infer<typeof productSchema>;",
    "```",
    
    "## 4. Core Extraction Logic (server/lib/extract-products.ts)",
    "- Create a class `ProductExtractor` with:",
    "  - `MAX_PDF_SIZE_MB = 5` constant to skip oversized files",
    "  - `DEEPSEEK_TIMEOUT_MS = 25000` for AbortController timeout handling",
    "  - `TOKEN_CHUNK_SIZE = 3500` for splitting large PDF texts",
    "",
    "- Implement `extractTextFromPdf(pdfPath: string): Promise<string>` using pdf-parse with error handling:",
    "```typescript",
    "try {",
    "  const dataBuffer = fs.readFileSync(pdfPath);",
    "  const data = await pdf(dataBuffer);",
    "  return data.text.trim();",
    "} catch (err) {",
    "  console.error(`Failed to parse ${pdfPath}:`, err);",
    "  return '';",
    "}",
    "```",
    "",
    "- Implement `chunkText(text: string, maxTokens: number): string[]`:",
    "  - Split by paragraphs first, then by sentences if needed",
    "  - Ensure no chunk exceeds ~3500 tokens (approx. 14k chars)",
    "  - Preserve context by keeping section headers in each chunk",
    "",
    "- Implement `callDeepSeek(textChunk: string, signal: AbortSignal): Promise<ProductInput[]>`:",
    "```typescript",
    "const response = await fetch('https://api.deepseek.com/v1/chat/completions', {",
    "  method: 'POST',",
    "  headers: {",
    "    'Content-Type': 'application/json',",
    "    'Authorization': `Bearer ${process.env.DEEPSEEK_API_KEY}`",
    "  },",
    "  signal,",
    "  body: JSON.stringify({",
    "    model: 'deepseek-chat',",
    "    messages: [",
    "      { role: 'system', content: `You are a helpful assistant that extracts menu items from restaurant menus. Return ONLY a valid JSON array of objects. Each object must have: name (string, 1-100 chars), description (string|null, max 500 chars), price (number in euros, 2 decimals), category (one of: Pizza|Pasta|Ice Cream|Drinks|Appetizers|Desserts|Other), allergens (array of strings), tags (array from: vegetarian|vegan|gluten-free|spicy|popular|seasonal), image_url (string URL|null). If uncertain, omit field rather than guess. Example: [{\"name\":\"Margherita\",\"description\":\"Classic tomato and mozzarella\",\"price\":8.50,\"category\":\"Pizza\",\"allergens\":[\"Lactose\",\"Gluten\"],\"tags\":[\"vegetarian\"],\"image_url\":null}]` },",
    "      { role: 'user', content: `Extract products from this menu text:\\n\\n${textChunk}` }",
    "    ],",
    "    temperature: 0.1,",
    "    response_format: { type: 'json_object' }",
    "  })",
    "});",
    "if (!response.ok) throw new Error(`DeepSeek API error: ${response.status}`);",
    "const result = await response.json();",
    "// The API returns a JSON object; we expect the content to be a JSON array string",
    "const content = result.choices?.[0]?.message?.content;",
    "if (!content) return [];",
    "const products = JSON.parse(content); // content should be the array",
    "return Array.isArray(products) ? products : [];",
    "```",
    "",
    "- Implement `upsertProducts(db: NeonQueryFn, products: ProductInput[], dryRun = false): Promise<void>`:",
    "```typescript",
    "for (const product of products) {",
    "  const validated = productSchema.safeParse(product);",
    "  if (!validated.success) {",
    "    console.warn('Invalid product skipped:', validated.error, product);",
    "    continue;",
    "  }",
    "  if (dryRun) {",
    "    console.log(`[DRY RUN] Would upsert: ${validated.data.name} (${validated.data.category})`);",
    "    continue;",
    "  }",
    "  await db.insert(products)",
    "    .values(validated.data)",
    "    .onConflictDoUpdate({",
    "      target: [products.name, products.category],",
    "      set: {",
    "        price: validated.data.price,",
    "        description: validated.data.description,",
    "        allergens: validated.data.allergens,",
    "        tags: validated.data.tags,",
    "        imageUrl: validated.data.imageUrl,",
    "        updatedAt: new Date()",
    "      }",
    "    });",
    "}",
    "```",
    "",
    "- Main `extractAll(dryRun = false)` method:",
    "  1. Read all PDFs from both directories using glob",
    "  2. For each PDF: check size limit → extract text → chunk if needed → call DeepSeek with AbortController timeout → validate → upsert",
    "  3. Log progress: `Processed 3/5 PDFs, inserted 24 products, skipped 2 (validation)`",
    "  4. After all chunks, optionally deduplicate products that may appear across chunks (by name+category) and keep the most complete version (e.g., one with description and image if available).",
    "  5. Return summary: `{ processed: number, inserted: number, skipped: number, errors: string[] }`",
    
    "## 5. CLI Script (scripts/extract-products.ts)",
    "```typescript",
    "#!/usr/bin/env tsx",
    "import { parseArgs } from 'node:util';",
    "import { ProductExtractor } from '../server/lib/extract-products';",
    "import { getDb } from '../server/db';",
    "",
    "const { values } = parseArgs({",
    "  args: process.argv.slice(2),",
    "  options: {",
    "    'dry-run': { type: 'boolean', default: false },",
    "    'category': { type: 'string' } // Optional: filter by category folder",
    "  }",
    "});",
    "",
    "async function main() {",
    "  if (!process.env.DEEPSEEK_API_KEY) {",
    "    console.error('❌ DEEPSEEK_API_KEY not set in Secrets');",
    "    process.exit(1);",
    "  }",
    "  const db = await getDb();",
    "  const extractor = new ProductExtractor(db);",
    "  const summary = await extractor.extractAll(values['dry-run']);",
    "  console.log('✅ Extraction complete:', summary);",
    "  process.exit(summary.errors.length > 0 ? 1 : 0);",
    "}",
    "main().catch(err => { console.error(err); process.exit(1); });",
    "```",
    
    "## 6. API Endpoint (server/routes/products.ts)",
    "- Create `GET /api/products` with:",
    "  - Query params: `?category=Pizza&search=margherita&limit=50`",
    "  - ILIKE search on name/description, exact match on category",
    "  - Tag filtering: `?tags=vegetarian,gluten-free` (AND logic)",
    "  - Response caching headers: `Cache-Control: public, s-maxage=300, stale-while-revalidate=60`",
    "  - ETag support for conditional requests",
    "```typescript",
    "router.get('/api/products', async (req, res) => {",
    "  const { category, search, tags, limit = 50 } = req.query;",
    "  // Build query with drizzle, add indexes usage hints",
    "  // Return { products: [...], meta: { total, filters, cached: boolean } }",
    "});",
    "```",
    
    "## 7. Frontend Integration (React Query)",
    "- In client code, use `useQuery` with:",
    "  - `staleTime: 5 * 60 * 1000` (5 minutes)",
    "  - `gcTime: 30 * 60 * 1000` (30 minutes)",
    "  - Automatic refetch on window focus with `refetchOnWindowFocus: true`",
    "  - Optimistic updates for cart interactions",
    
    "## 8. Error Handling & Logging",
    "- Log all DeepSeek API calls at debug level only (respect LOG_LEVEL)",
    "- On API failure: exponential backoff retry (max 2 attempts). Use `setTimeout` with increasing delays (e.g., 1s, 4s) before retrying the same chunk.",
    "- On validation failure: log skipped items with reason, continue processing",
    "- On DB error: rollback transaction, log structured error with order_id context (if applicable)",
    
    "## 9. Security & Rate Limiting",
    "- The extraction script runs only via CLI, not exposed to web",
    "- API endpoint is public but rate-limited via existing middleware (Prompt 5)",
    "- DeepSeek API key never exposed to client; server-side only",
    
    "## 10. Documentation Updates",
    "- Add to README.md:",
    "  ```markdown",
    "  ## Product Extraction",
    "  - Place menu PDFs in `knowledge-base/menu/` or `knowledge-base/icecream/`",
    "  - Run extraction: `npm run extract-products`",
    "  - Test first: `npm run extract-products:dry-run`",
    "  - Required Secret: `DEEPSEEK_API_KEY`",
    "  - Limits: Max 5MB per PDF, 25s timeout per API call",
    "  ```"
  ],
  "replit_note": "DeepSeek API calls incur costs; extraction runs on-demand only. Ensure DEEPSEEK_API_KEY is set in Replit Secrets. Free tier has ~30s request timeout – the 25s AbortController provides safety margin. PDFs >5MB are skipped with warning. Use --dry-run to test without DB writes. Products table uses ON CONFLICT for idempotent re-runs.",
  "testing_checkpoint": [
    "1. Run `npm run extract-products:dry-run` – verify console output shows products that would be inserted",
    "2. Run `npm run extract-products` – check database: `SELECT COUNT(*) FROM products;` returns expected count",
    "3. Call `GET /api/products` – verify JSON structure matches schema, caching headers present",
    "4. Test filters: `?category=Ice Cream`, `?search=vanilla`, `?tags=vegetarian` – results match expectations",
    "5. Re-run extraction with same PDFs – verify no duplicates (ON CONFLICT update works)",
    "6. Simulate DeepSeek timeout: mock fetch to delay >25s – verify AbortController triggers, error logged gracefully",
    "7. If PDFs are large and chunked, ensure no product appears twice after deduplication step."
  ]
}