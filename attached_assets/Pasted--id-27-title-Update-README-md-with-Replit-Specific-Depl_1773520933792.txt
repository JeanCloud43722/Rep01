{
  "id": 27,
  "title": "Update README.md with Replit-Specific Deployment Checklist",
  "priority": "P3-Low",
  "depends_on": [],
  "context": "No documented checklist for production deployment on Replit. New developers may miss critical setup steps.",
  "task": "Update README.md with a comprehensive Replit-specific deployment checklist covering all prompts.",
  "detailed_instructions": [
    "Add section '## Deployment on Replit' to README.md.",
    "Pre-deployment checklist:",
    "- Bind PostgreSQL database in Tools > Database",
    "- Set all Secrets: DATABASE_URL (auto-set), SESSION_SECRET (64+ hex chars), VAPID_PUBLIC_KEY and VAPID_PRIVATE_KEY (from npx web-push generate-vapid-keys), ADMIN_PASSWORD (strong unique password), ALLOWED_ORIGIN (comma-separated allowed origins), LOG_LEVEL ('info' for production)",
    "- Run npm install to install all new dependencies",
    "- Verify Drizzle migrations run on startup (check server logs for 'Migrations complete')",
    "- Run npm test to verify all API tests pass",
    "First run checklist:",
    "- Login with admin/admin123. IMMEDIATELY change default admin password",
    "- Create test order. Open customer page via QR code",
    "- Test push notification on mobile device (install PWA first on iOS)",
    "- Test audio cues on both admin and customer pages",
    "- Test state machine: try invalid transition — get clear error",
    "- Test service request acknowledgment: customer calls waiter, admin acknowledges",
    "Post-deployment:",
    "- Set up external uptime monitor to ping /api/health every 5 minutes",
    "- Watch logs for logger.error entries — investigate any database or push failures",
    "- Test rate limiting by spamming endpoints (should get 429 after threshold)",
    "- Verify auto-cleanup runs hourly (check logs for 'Auto-cleanup' messages)",
    "- Periodically review session table size (pruneSessionInterval should keep it small)",
    "- Monitor database size in Replit Database panel — should stay under 1GB with cleanup",
    "Add section '## Architecture Overview' with brief descriptions of each module and link to full ARCHITECTURE.md.",
    "Add section '## Environment Variables' with table of all variables, whether required/optional, and defaults.",
    "Add section '## Development' with commands: npm run dev, npm run build, npx drizzle-kit generate."
  ],
  "replit_note": "This checklist ensures consistent, error-free deployments and helps new team members onboard quickly on Replit.",
  "testing_checkpoint": "Follow the checklist step-by-step on a fresh Replit fork — everything works as described."
}