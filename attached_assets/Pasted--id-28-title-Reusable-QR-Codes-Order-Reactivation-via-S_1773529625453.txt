{
  "id": 28,
  "title": "Reusable QR Codes — Order Reactivation via Schedule Button",
  "priority": "P1-High",
  "depends_on": [1, 10, 23, 25],
  "context": "Currently, once an order is marked 'completed', the QR code becomes useless for that table/customer. Restaurants need to reuse the same QR code across multiple order cycles (e.g., same table orders appetizer, then main course, then dessert). The Schedule button should reactivate completed orders, and messages should also be able to schedule/terminate the buzzer.",
  "task": "Enable order reactivation from 'completed' status back to 'waiting' or 'subscribed', allow Schedule endpoint to work on completed orders, and ensure QR codes remain valid indefinitely.",
  "detailed_instructions": [
    "STATE MACHINE UPDATE: In server/lib/state-machine.ts, update VALID_TRANSITIONS:",
    "  - Add 'completed' -> ['waiting', 'subscribed'] (reactivation paths)",
    "  - This allows staff to reactivate a completed order for a new round",
    "  - Export a new function: canReactivate(order: Order): boolean — returns true if order.status === 'completed'",
    "",
    "SCHEDULE ENDPOINT UPDATE: In server/routes.ts, modify POST /api/orders/:id/schedule:",
    "  - Remove any status checks that block scheduling on 'completed' orders",
    "  - When scheduling on a 'completed' order: automatically transition status to 'scheduled'",
    "  - Clear old notification data: set notifiedAt to null, clear old service requests (optional)",
    "  - Log: 'Reactivated completed order {orderId} for new scheduling'",
    "  - Fire notifyOrderUpdate(id, 'status_update') + notifyAdminUpdate(id, 'status_update')",
    "",
    "NEW REACTIVATION ENDPOINT: Add POST /api/orders/:id/reactivate (protected by requireAuth):",
    "  - Body validated with: z.object({ resetMessages: z.boolean().optional().default(false) })",
    "  - Transitions order from 'completed' to 'waiting'",
    "  - If resetMessages === true: clear messages[], serviceRequests[], offers[] arrays (keep notes)",
    "  - If resetMessages === false: preserve history (customer sees full conversation)",
    "  - Reset notifiedAt to null, scheduledTime to null",
    "  - Generate new QR code URL with same orderId (QR remains valid)",
    "  - Fire notifyAdminUpdate(id, 'order_reactivated')",
    "  - Return updated Order",
    "",
    "ADMIN UI — REACTIVATE BUTTON: In admin.tsx order cards:",
    "  - For orders with status 'completed': show 'Reactivate' button (blue, RotateCcw icon)",
    "  - On click: open confirmation dialog with checkbox 'Clear message history?'",
    "  - On confirm: call POST /api/orders/:id/reactivate with resetMessages value",
    "  - Show success toast: 'Order reactivated — QR code is active again'",
    "  - Also allow 'Schedule' button on completed orders (existing button, now enabled)",
    "",
    "CUSTOMER PAGE — REACTIVATED ORDER HANDLING: In customer.tsx:",
    "  - When order status changes from 'completed' to 'waiting'/'subscribed'/'scheduled':",
    "    - Show toast notification: 'Order reactivated — you can now receive updates'",
    "    - Reset StatusCard to show appropriate status (not 'Order Ready')",
    "    - If messages were cleared: show 'New order round started' banner",
    "    - If messages preserved: show 'Continuing conversation' banner",
    "  - AudioContext and push subscription should remain active (no re-subscription needed)",
    "  - WebSocket connection remains open (no reconnect needed)",
    "",
    "QR CODE PERSISTENCE: Verify QR code behavior:",
    "  - QR code URL is /order/{orderId} — this never changes for an order",
    "  - When order is reactivated, the same QR code should work immediately",
    "  - Test: Complete order → reactivate → scan QR → customer page loads with new status",
    "  - No QR code regeneration needed — orderId stays the same",
    "",
    "MESSAGE-TRIGGERED SCHEDULING: In POST /api/orders/:id/message endpoint:",
    "  - Add optional body field: scheduledTime (ISO 8601 string, optional)",
    "  - If scheduledTime is provided and order is 'completed': transition to 'scheduled'",
    "  - This allows staff to send a message AND schedule a follow-up buzzer in one action",
    "  - Example: 'Your dessert will be ready in 10 minutes' + schedule notification for +10min",
    "  - Validate: scheduledTime must be in the future",
    "  - Create node-schedule job as per existing schedule logic",
    "",
    "ANALYTICS TRACKING: Add reactivation tracking:",
    "  - In orders table, add: reactivationCount (integer default 0), lastReactivatedAt (timestamp nullable)",
    "  - On each reactivation: increment reactivationCount, set lastReactivatedAt to now()",
    "  - In admin.tsx order cards: show 'Round N' badge where N = reactivationCount + 1",
    "  - In /api/health endpoint: include avgReactivationsPerOrder metric",
    "",
    "AUTO-CLEANUP EXCEPTION: In Prompt 25's auto-cleanup logic:",
    "  - Add condition: DO NOT delete orders where reactivationCount > 0 AND lastReactivatedAt < 7 days",
    "  - Recently reactivated orders should be preserved longer for analytics",
    "  - Default cleanup still applies to orders with reactivationCount === 0"
  ],
  "replit_note": "This feature enables true table-side ordering where the same QR code serves multiple order rounds. Test the full cycle: create → notify → complete → reactivate → schedule → notify → complete. The same QR code should work throughout.",
  "testing_checkpoint": "Complete an order. Click 'Reactivate' — order returns to 'waiting'. Scan same QR code — customer page loads with new status. Send message with scheduledTime — order transitions to 'scheduled'. Auto-cleanup preserves reactivated orders for 7 days."
}