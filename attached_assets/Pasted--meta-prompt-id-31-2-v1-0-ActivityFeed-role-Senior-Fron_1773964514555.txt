{
  "meta": {
    "prompt_id": "31.2-v1.0-ActivityFeed",
    "role": "Senior Frontend Engineer",
    "purpose": "Create a persistent notification center that tracks WebSocket events for operational awareness.",
    "prerequisites": [
      "Prompt 31.1 is complete (header cleanup + urgency sorting)",
      "WebSocket events are received in use-admin-websocket.ts (message_received, service_request, order_status_changed)",
      "shadcn/ui Sheet component is available OR pure Tailwind fallback is acceptable"
    ],
    "scope": "STRICTLY: Create new component client/src/components/admin/ActivityFeed.tsx + integrate into admin.tsx. NO backend changes."
  },
  
  "critical_directive": "⚠️ SENIOR ENGINEER MODE: This is an ADDITIVE prompt. Do NOT modify existing sorting or header logic from Prompt 31.1. Focus ONLY on the ActivityFeed component and its integration. If shadcn/ui Sheet is missing, implement a simple slide-in panel with Tailwind.",
  
  "deliverables": [
    {
      "file": "client/src/components/admin/ActivityFeed.tsx",
      "task": "Create slide-out event history panel with actionable items",
      "code_requirements": [
        "import { Sheet, SheetContent, SheetTrigger, SheetHeader, SheetTitle } from '@/components/ui/sheet';",
        "import { Bell, MessageSquare, AlertCircle, CheckCircle } from 'lucide-react';",
        "",
        "export type AppEvent = {",
        "  id: string;",
        "  type: 'message' | 'service_request' | 'order_status' | 'order_created';",
        "  orderId: string;",
        "  title: string;",
        "  preview: string;",
        "  timestamp: string; // ISO string",
        "  read: boolean;",
        "  meta?: Record<string, any>;",
        "};",
        "",
        "export function ActivityFeed({",
        "  isOpen,",
        "  onOpenChange,",
        "  events,",
        "  onEventClick,",
        "}: {",
        "  isOpen: boolean;",
        "  onOpenChange: (open: boolean) => void;",
        "  events: AppEvent[];",
        "  onEventClick: (event: AppEvent) => void;",
        "}) {",
        "  return (",
        "    <Sheet open={isOpen} onOpenChange={onOpenChange}>",
        "      <SheetContent side=\"right\" className=\"w-full sm:w-96 p-0\">",
        "        <SheetHeader className=\"p-4 border-b\">",
        "          <SheetTitle className=\"flex items-center gap-2\">",
        "            <Bell className=\"w-5 h-5\" /> Activity Feed",
        "          </SheetTitle>",
        "          <p className=\"text-sm text-gray-500\">",
        "            {events.filter(e => !e.read).length} unread",
        "          </p>",
        "        </SheetHeader>",
        "        ",
        "        <div className=\"overflow-y-auto h-[calc(100vh-80px)]\">",
        "          {events.length === 0 ? (",
        "            <div className=\"p-8 text-center text-gray-500\">",
        "              No recent activity",
        "            </div>",
        "          ) : (",
        "            <ul className=\"divide-y\">",
        "              {events.slice(0, 50).map((event) => (",
        "                <li",
        "                  key={event.id}",
        "                  className={`p-4 hover:bg-gray-50 cursor-pointer transition-colors ${",
        "                    !event.read ? 'bg-blue-50/50' : ''",
        "                  }`}",
        "                  onClick={() => {",
        "                    onEventClick(event);",
        "                    // Mark as read locally (backend sync optional)",
        "                  }}",
        "                >",
        "                  <div className=\"flex items-start gap-3\">",
        "                    {/* Icon by event type */}",
        "                    <span className={`p-2 rounded-full ${",
        "                      event.type === 'service_request' ? 'bg-red-100 text-red-600' :",
        "                      event.type === 'message' ? 'bg-blue-100 text-blue-600' :",
        "                      'bg-gray-100 text-gray-600'",
        "                    }`}>",
        "                      {event.type === 'service_request' && <AlertCircle className=\"w-4 h-4\" />}",
        "                      {event.type === 'message' && <MessageSquare className=\"w-4 h-4\" />}",
        "                      {event.type === 'order_status' && <CheckCircle className=\"w-4 h-4\" />}",
        "                      {event.type === 'order_created' && <Bell className=\"w-4 h-4\" />}",
        "                    </span>",
        "                    ",
        "                    <div className=\"flex-1 min-w-0\">",
        "                      <div className=\"flex justify-between items-start\">",
        "                        <p className=\"font-medium text-sm truncate\">{event.title}</p>",
        "                        <span className=\"text-xs text-gray-400 whitespace-nowrap ml-2\">",
        "                          {new Date(event.timestamp).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' })}",
        "                        </span>",
        "                      </div>",
        "                      <p className=\"text-sm text-gray-600 truncate mt-1\">{event.preview}</p>",
        "                      {!event.read && (",
        "                        <span className=\"inline-block w-2 h-2 bg-blue-500 rounded-full mt-2\" />",
        "                      )}",
        "                    </div>",
        "                  </div>",
        "                </li>",
        "              ))}",
        "            </ul>",
        "          )}",
        "        </div>",
        "      </SheetContent>",
        "    </Sheet>",
        "  );",
        "}",
        "",
        "// Fallback if shadcn/ui Sheet is not available:",
        "// Use a fixed-position div with transform transition for slide-in effect",
        "// See tailwind classes: fixed right-0 top-0 h-full w-80 bg-white shadow-xl transform transition-transform duration-300",
        "// Add state: const [isOpen, setIsOpen] = useState(false);",
        "// Toggle with: className={isOpen ? 'translate-x-0' : 'translate-x-full'}"
      ]
    },
    {
      "file": "client/src/pages/admin.tsx",
      "task": "Integrate ActivityFeed with WebSocket event listener",
      "code_requirements": [
        "// 1. Import and state setup",
        "import { ActivityFeed, AppEvent } from '@/components/admin/ActivityFeed';",
        "const [feedEvents, setFeedEvents] = useState<AppEvent[]>([]);",
        "const [isFeedOpen, setIsFeedOpen] = useState(false);",
        "const [unreadEventCount, setUnreadEventCount] = useState(0);",
        "",
        "// 2. WebSocket event handler (in use-admin-websocket.ts or inline)",
        "useEffect(() => {",
        "  const handleWsEvent = (payload: { type: string; orderId: string; data: any }) => {",
        "    // Map WebSocket payload to AppEvent",
        "    const newEvent: AppEvent = {",
        "      id: `${payload.type}-${payload.orderId}-${Date.now()}`,",
        "      type: payload.type as AppEvent['type'],",
        "      orderId: payload.orderId,",
        "      title: getEventTitle(payload.type, payload.data),",
        "      preview: getEventPreview(payload.type, payload.data),",
        "      timestamp: new Date().toISOString(),",
        "      read: false,",
        "      meta: payload.data",
        "    };",
        "    ",
        "    // Append to feed (keep max 50)",
        "    setFeedEvents(prev => [newEvent, ...prev].slice(0, 50));",
        "    setUnreadEventCount(prev => prev + 1);",
        "    ",
        "    // Still show ephemeral toast for immediate awareness",
        "    showToast({",
        "      title: newEvent.title,",
        "      description: newEvent.preview,",
        "      action: {",
        "        label: 'Reply',",
        "        onClick: () => {",
        "          openMessageModal(payload.orderId);",
        "          // Mark event as read",
        "          setFeedEvents(prev => prev.map(e =>",
        "            e.id === newEvent.id ? { ...e, read: true } : e",
        "          ));",
        "        }",
        "      }",
        "    });",
        "  };",
        "  ",
        "  // Subscribe to WebSocket events",
        "  const unsubscribe = wsManager.subscribe('admin:events', handleWsEvent);",
        "  return () => unsubscribe();",
        "}, []);",
        "",
        "// Helper functions for event formatting",
        "const getEventTitle = (type: string, data: any): string => {",
        "  switch (type) {",
        "    case 'service_request': return `🔔 Table ${data.tableNumber} needs service`;",
        "    case 'message': return `💬 New message from Order ${data.orderId.slice(-4)}`;",
        "    case 'order_status': return `📦 Order ${data.orderId.slice(-4)}: ${data.newStatus}`;",
        "    case 'order_created': return `🆕 New Order ${data.orderId.slice(-4)}`;",
        "    default: return 'Activity Update';",
        "  }",
        "};",
        "",
        "const getEventPreview = (type: string, data: any): string => {",
        "  if (type === 'message') return data.message?.slice(0, 50) + (data.message?.length > 50 ? '...' : '');",
        "  if (type === 'service_request') return data.reason || 'Customer requested assistance';",
        "  return '';",
        "};",
        "",
        "// 3. Render ActivityFeed in admin.tsx",
        "// Add to JSX (near header):",
        "<ActivityFeed",
        "  isOpen={isFeedOpen}",
        "  onOpenChange={setIsFeedOpen}",
        "  events={feedEvents}",
        "  onEventClick={(event) => {",
        "    // Navigate to order and open relevant modal",
        "    if (event.type === 'message') {",
        "      openMessageModal(event.orderId);",
        "    } else if (event.type === 'service_request') {",
        "      openOrderDetails(event.orderId);",
        "    }",
        "    // Mark as read",
        "    setFeedEvents(prev => prev.map(e =>",
        "      e.id === event.id ? { ...e, read: true } : e",
        "    ));",
        "    setUnreadEventCount(prev => Math.max(0, prev - 1));",
        "  }}",
        "/>",
        "",
        "// 4. Connect bell icon in header to open feed",
        "// In header Bell button onClick:",
        "onClick={() => {",
        "  setIsFeedOpen(true);",
        "  // Mark all as read when opening",
        "  setFeedEvents(prev => prev.map(e => ({ ...e, read: true })));",
        "  setUnreadEventCount(0);",
        "}}"
      ]
    }
  ],
  
  "testing_checkpoints": [
    {
      "step": 1,
      "command": "Open admin dashboard, click Bell icon in header",
      "expected": "ActivityFeed slides in from right. Shows 'No recent activity' if no events yet."
    },
    {
      "step": 2,
      "command": "Simulate customer message via WebSocket (or test tool)",
      "expected": "Toast appears with 'Reply' button. ActivityFeed shows new event with blue background (unread). Bell icon shows badge '1'."
    },
    {
      "step": 3,
      "command": "Click event in ActivityFeed",
      "expected": "Feed closes. MessageModal opens for that order. Event is marked read (no more blue background)."
    },
    {
      "step": 4,
      "command": "Generate 55 events rapidly (test script)",
      "expected": "Feed shows only latest 50 events (slice(0, 50)). No performance lag."
    },
    {
      "step": 5,
      "command": "Run npm test",
      "expected": "All pre-existing tests pass; no regression from ActivityFeed integration."
    }
  ],
  
  "replit_specific_notes": [
    "If shadcn/ui Sheet is not installed: implement fallback with Tailwind fixed-position panel + transform transition.",
    "WebSocket event mapping: ensure payload structure matches what backend sends (orderId, type, data).",
    "Performance: limit feed to 50 items to prevent memory growth during long shifts.",
    "Accessibility: add aria-live=\"polite\" to toast announcements for screen readers."
  ],
  
  "success_criteria": "Prompt 31.2 is COMPLETE when:\n1. ActivityFeed component renders as slide-in panel (Sheet or Tailwind fallback)\n2. WebSocket events are mapped to AppEvent and appended to feed (max 50)\n3. Unread events have visual indicator (blue background + dot)\n4. Clicking event navigates to order and opens relevant modal\n5. Bell icon in header shows unread count badge\n6. Toasts still appear with 'Reply' action for messages\n7. All 5 testing checkpoints pass\n8. Zero TypeScript errors (npx tsc --noEmit)\n9. All pre-existing tests still pass",
  
  "failure_handling": "If ActivityFeed doesn't receive events:\n1. Verify WebSocket subscription in use-admin-websocket.ts is active\n2. Check event payload structure matches expected format\n3. Add console.log in handleWsEvent to debug incoming data\n4. If Sheet component fails: use Tailwind fallback immediately – do NOT block progress\n5. Wait for explicit confirmation before proceeding to Prompt 31.3",
  
  "final_self_verification": "Before marking complete:\n□ Does ActivityFeed slide in from right when Bell is clicked?\n□ Do WebSocket events appear in feed within 1 second?\n□ Do unread events have blue background + dot indicator?\n□ Does clicking event open correct modal for that order?\n□ Does Bell icon show unread count badge?\n□ Do toasts still appear with 'Reply' action?\n□ Is feed limited to 50 items (no memory leak)?\n□ Does npx tsc --noEmit return 0 errors?\n□ Do all pre-existing tests still pass?\n\nIf ANY answer is NO: fix before proceeding."
}