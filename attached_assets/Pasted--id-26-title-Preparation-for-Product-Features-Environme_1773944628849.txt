{
  "id": 26,
  "title": "Preparation for Product Features – Environment Setup & Dependency Installation",
  "priority": "P0-Critical",
  "context": "Before implementing the product catalog, AI ordering, and related features, we need to ensure the environment is correctly set up: required folders exist, dependencies are installed, environment variables are defined, and the existing system is ready to be extended.",
  "task": "Prepare the Replit project by creating necessary directories, installing dependencies, validating environment variables, and running health checks to ensure a solid foundation for Prompts 27–29.",
  "detailed_instructions": [
    "## 1. Directory Structure",
    "- Create the following folders if they don't exist:",
    "  - `knowledge-base/menu/`",
    "  - `knowledge-base/icecream/`",
    "  - `scripts/` (for extraction scripts)",
    "  - `server/lib/` (for new utilities)",
    "  - `client/src/components/ProductCatalog/`",
    "  - `client/src/hooks/` (if not present)",
    "",
    "## 2. Install Required Dependencies",
    "- Run the following command to install all necessary packages:",
    "  ```bash",
    "  npm install pdf-parse glob zod openai lucide-react use-debounce @tanstack/react-query focus-trap-react",
    "  ```",
    "- Ensure `tsx` is installed globally or as dev dependency (already present if using `npm run dev`).",
    "- Verify that `@types/node` is installed (for `fs` and `path` types).",
    "",
    "## 3. Environment Variables Validation",
    "- Add the following to your `.env` file or Replit Secrets:",
    "  ```",
    "  DEEPSEEK_API_KEY=your_key_here",
    "  ```",
    "- If not already set, ensure `DATABASE_URL`, `SESSION_SECRET`, `VAPID_*` are present (from earlier prompts).",
    "- Create a quick validation script `scripts/check-env.ts` that checks for `DEEPSEEK_API_KEY` and logs a warning if missing. This script can be run manually or as part of the build process.",
    "",
    "## 4. Verify Existing System Health",
    "- Run the test suite to ensure all existing tests pass:",
    "  ```bash",
    "  npx vitest run",
    "  ```",
    "- Expected: 40+ tests passing, especially those covering orders, auth, messages, and state machine.",
    "- If any tests fail, address them before proceeding.",
    "",
    "## 5. Create Utility Files Placeholders",
    "- Create placeholder files to be filled by later prompts:",
    "  - `server/lib/extract-products.ts` (will be implemented in Prompt 27)",
    "  - `server/lib/chat-tools.ts` (Prompt 29)",
    "  - `server/lib/parse-order-json.ts` (Prompt 29)",
    "  - `server/lib/prompts.ts` (Prompt 29)",
    "  - `server/routes/chat.ts` (Prompt 29)",
    "  - `server/routes/products.ts` (Prompt 27)",
    "  - `client/src/components/ProductCatalog/index.ts` (Prompt 28)",
    "- Add minimal exports to avoid TypeScript errors during development.",
    "",
    "## 6. Database Migration Readiness",
    "- Ensure Drizzle migrations are set up and can be applied:",
    "  ```bash",
    "  npx drizzle-kit generate",
    "  ```",
    "- The migrations folder should exist and be applied on server start (as per Prompt 1).",
    "",
    "## 7. Replit-Specific Configuration",
    "- If not already done, bind a PostgreSQL database via Replit's Database tool.",
    "- Confirm that all secrets are set in Replit Secrets (not just `.env`).",
    "- Ensure the `ALLOWED_ORIGIN` includes your Replit preview domain (e.g., `https://yourproject.yourusername.repl.co`).",
    "",
    "## 8. Final Readiness Check",
    "- Run a quick manual test:",
    "  - Start the server with `npm run dev`.",
    "  - Visit the admin page (`/admin`) and log in.",
    "  - Create a test order and open the customer page.",
    "  - Verify that basic chat and order status work.",
    "- All these should function as before; if anything is broken, fix it now."
  ],
  "replit_note": "This prompt sets the stage for the new features. It's essential that the existing system is stable and that all secrets are correctly configured. The `DEEPSEEK_API_KEY` must be obtained from the DeepSeek platform (free credits available). If you encounter any issues with dependency installation, check the Node version (Replit uses Node 18+).",
  "testing_checkpoint": [
    "1. Run `npm install` – all packages install without errors.",
    "2. Run `npx vitest run` – all existing tests pass.",
    "3. Check that `knowledge-base/menu/` and `knowledge-base/icecream/` folders exist.",
    "4. Verify `DEEPSEEK_API_KEY` is set in Secrets.",
    "5. Start the server – no startup errors, health endpoint returns OK.",
    "6. Create a test order and verify customer page loads correctly."
  ]
}