{
  "id": 26,
  "title": "Set Up Vitest for Server-Side API Testing",
  "priority": "P2-Medium",
  "depends_on": [2, 6, 13, 24],
  "context": "Zero test coverage means regressions are caught only by manual testing. Vitest is Vite-native and works well on Replit.",
  "task": "Set up Vitest for server-side API testing and write tests for critical paths.",
  "detailed_instructions": [
    "Install vitest, supertest, and @types/supertest as dev dependencies.",
    "Create vitest.config.ts at project root: { test: { globals: true, environment: 'node', include: ['server/**/*.test.ts'], setupFiles: ['./server/test-setup.ts'] } }.",
    "Create server/test-setup.ts: set up test database connection using DATABASE_URL with a test-specific database or schema. Add beforeAll to run migrations, afterAll to close connection.",
    "Add script to package.json: 'test': 'vitest run', 'test:watch': 'vitest'.",
    "Create server/__tests__/orders.test.ts with tests for:",
    "- POST /api/orders → creates order with 'waiting' status, returns 201",
    "- GET /api/orders/:id → returns order by ID, returns 404 for non-existent",
    "- DELETE /api/orders/:id → deletes order, returns 204, returns 404 for non-existent",
    "- POST /api/orders/:id/register → transitions waiting to subscribed",
    "- POST /api/orders/:id/register on non-waiting order → idempotent, no error",
    "Create server/__tests__/state-machine.test.ts with tests for:",
    "- Valid transitions: waiting->subscribed->scheduled->notified->completed",
    "- Invalid transition: waiting->completed → error",
    "- Invalid transition: notified->subscribed → error",
    "- Direct notify: subscribed->notified → succeeds",
    "Create server/__tests__/auth.test.ts with tests for:",
    "- POST /api/auth/login with correct password → 200 + session",
    "- POST /api/auth/login with wrong password → 401",
    "- GET /api/orders without session → 401",
    "- GET /api/orders/:id without session → 200 (customer route)",
    "Create server/__tests__/messages.test.ts with tests for:",
    "- POST /api/orders/:id/message → adds staff message",
    "- POST /api/orders/:id/customer-message → adds customer message",
    "- Message validation: empty message → 400, message > 200 chars → 400",
    "Keep tests focused on happy paths and critical error paths. Do NOT test WebSocket or push notifications (too complex for initial test suite)."
  ],
  "replit_note": "Run tests with 'npm test' in Replit Shell. Vitest is fast and Vite-native. Use the test database to avoid polluting production data. Tests run in ~5 seconds for this scope.",
  "testing_checkpoint": "Run npm test — all tests pass. Add a broken state transition in code — state-machine test fails. Fix it — tests pass again. This is the baseline test suite to build upon."
}